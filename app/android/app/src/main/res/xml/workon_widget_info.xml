<?xml version="1.0" encoding="utf-8"?>
<!-- 작업온 홈 위젯: 2x2(미수금 중심) 기본, 4x2(일정+미수금)로 리사이즈 가능.
     갱신은 앱이 데이터 로드 시 트리거(네트워크 호출 없음) → updatePeriodMillis=0. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="200dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/workon_widget_small"
    android:previewLayout="@layout/workon_widget_medium"
    android:description="@string/workon_widget_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
