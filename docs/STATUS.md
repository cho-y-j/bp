# 현재 상태 (STATUS)

> 세션 시작 시 이 파일을 가장 먼저 읽는다. 항상 최신 상태를 유지한다.

## 프로젝트
- 작업지시자–작업자 연결 + 서류 공유 + 작업확인서 + 정산/수금 장부 + 만료 관리 앱 (무료 제공 목표)
- 상세 요구사항은 `PROJECT.md` 참조

## 완료된 것
- 2026-07-10: 작업 규칙(CLAUDE.md) 및 문서 구조(docs/) 초기 설정
- 2026-07-11: 요구사항 1차 정리 → PROJECT.md 기록
- 2026-07-11: SKEP 레퍼런스 분석 완료 (`worklog/2026-07-11-skep-분석.md`) — 차용/배제 항목 확정
- 2026-07-11: 추가 요구 반영 — 폭염 안전 알림, 세금계산서 자동 발행, "복잡성보다 자동화" 철학
- 2026-07-17: **웹 작업자 모드 `/w/*` 신설** (`worklog/2026-07-17-웹작업자모드.md`) — web/ 만 수정(backend·app 무수정, 기존 API만). 로그인 후 분기(사업장 미보유→`/w/home`, 게이트는 `/biz` 직접 접근 시만) + 작업자↔사업장 상호 전환 링크. `/w/home`(월 요약·최근 확인서·만료 서류·알림), `/w/confirmations`(작성 폼[연결/수기 상대·단가유형·**공수 0.5스텝**·장비·미리보기]+PaperConfirmation 상세·send 링크복사·PDF·revoke), `/w/ledger`(회사별 미수·부분입금·autoRemind 토글·수동독촉·명세서 PDF), `/w/documents`(업로드·묶음 공유·공유 관리; **마스킹은 앱 백로그**), `/w/income`(소득 리포트 연/월별·상대별·PDF + 세금계산서 준비 복사·mark). biz 레이아웃 CSS/컴포넌트 재사용, 한국어. **검증**: `npm run build` 타입에러 0(18라우트), 격리 스택(임시 pg 5436·api 3040·web 3005=web 사본, 사용자 `web/.next` 무접촉) playwright 실측 전 플로우(로그인→홈 자동진입→공수 1.5 확인서 270,000→장부 반영→부분입금 170,000→서류 업로드 D-19→묶음 링크→소득 리포트) + 스크린샷 8장(`web/screenshots-w-mode/`) 눈검수(데스크톱 1280·모바일 390). **사용자 테스트 스택(3001/3010/5439) 무접촉 재확인**, 검증 자원(5436/3040/3005) 정리, live-db(5432) 미사용.
- 2026-07-17: **P5a 사업장 강화 3종 API (backend)** (`worklog/2026-07-17-P5a-사업장백엔드.md`) — backend 만(app/web 무수정). 마이그레이션 1개(additive) `20260717130000_p5a_wage_marked_months`(Business.wageMarkedMonths). **① `GET /biz/site-costs?from=&to=&businessId?=`(+/pdf)** — SIGNED 확인서 현장별 집계(작업자별·팀 합계[인원수]·연인원/공수/금액·현장소계·전체총계, 최대 12개월, 발주처 제출 PDF: 사업장·기간 헤더). **② `GET /biz/wage-statement?month=&businessId?=` + `POST .../mark`** — 지급(paidAt) 기준 작업자별 지급총액·건수·일수 + 소득 유형별 세액 동시 산출(3.3% 사업소득 / 일용근로: 일 15만 공제·6%×45%=실효 2.7%·**1일 세액 1,000원 미만 소액부징수 0**), 상수+주석 명시·"세무상담 아님·세무사 확인 권장" 노트·**주민번호 비수집(홈택스 직접입력)**·복사텍스트, 월 마감 표시(멱등·마감 후 조회 유지). **③ `GET /biz/today-attendance?businessId?=`** — 오늘(KST) jobs 현장별 그룹·상태(예정/수락/시작[시각·컨디션]/완료[시각], work_logs 조인)·인원 요약. 이름 마스킹·scopedBusinessId·미인증 401. **검증**: 단위 **184**(+20)·e2e **172**(+10) 통과, build/lint(신규파일) 0, 실서버 curl(pg 5438/api 3060)+pdftotext 실측(**일당 20만×3일→일용 소득세 1,350/일×3=4,050** 손계산 대조, mark 멱등, 출역 상태 3종). **임시 pg 5438·백엔드 3060 만 사용, live-db(5432)·사용자 테스트 스택(5439/3010/3001) 무접촉·무손상**. 다음: P5b(앱)·P5c(웹) 병렬.
- 2026-07-17: **P5b 사업장 강화 3종 화면 (app)** (`worklog/2026-07-17-P5b-앱.md`) — **app/ 만 수정**(P5a API 소비, backend·web 무수정). **① 출역 현황판** — 사업장 홈 **최상단 카드**(오늘 전체/출근/완료/미출근 4칸, 실패 시 숨김) → 탭 상세(현장별 그룹·작업자 상태 배지 5종·시작/예정 시각·컨디션 배지 OK/BAD, 당겨서 새로고침). **② 현장별 인건비** — 기간 프리셋(이번 달 기본·3/6/12개월) → 전체 총계 헤더(총액·연인원·공수·건수) → 현장 카드(소계·인원, 탭 펼침) → 작업자별 내역(팀 배지+팀원 N명·연인원·공수·금액) → **PDF 저장·공유(인증 blob→share_plus)**. **③ 지급명세서(월 마감)** — 월 선택 → 소득유형 토글(**사업소득 3.3% ↔ 일용근로** 세액·차인지급액 표시 전환, 서버 동시 산출 소비) → 작업자 카드(지급액·연인원·소득세/지방세/원천징수/차인 tabular) → copyText 복사 → **월 마감(멱등, marked 배지·버튼 비활성)** → 안내 노트 필수 표시(세무 전문가 확인 권장·주민번호 비수집·홈택스 직접 입력). 모델 13종·프로바이더 3·레포 2, 신규 **54키×6 ARB**(791키 패리티), 금액 전부 tabular. **검증**: analyze 0·test **114**(회귀 108+신규 6: 세액 표시 포맷·요약 카운트 매핑·fromJson 3종·토글 렌더), **별도 시뮬**(workon-p5b-verify 신규 생성 — booted iPhone 17 무점유)+임시 스택(pg 5438/api 3060) 시드(현장 2·작업자 상태 4종·팀 확인서·지급 87만+15만) → **drive All passed + 스크린샷 8장**(`app/screenshots/p5b_01~08*.png`) Read 눈검수 — 출역 4/2/1/2·인건비 총계 144만·3.3% 26,100/일용 4,050 **시드·손계산 일치**. 자원 정리(시뮬 삭제·5438/3060 해제), 사용자 스택(3010 health ok·3001 200·5439 Up)·live-db(5432) 무손상 실측.
- 2026-07-17: **P5 통합 검수 완료 — 판정: P5 완료 가능(치명·중요 결함 0건)** (`worklog/2026-07-17-P5-통합검수.md`). 독립 QA(구현 3배치와 분리)가 세액 독립 재검증·배치 경계·엣지·정책 일관성 검수. **회귀 5종 전부 재현**: backend 단위 **184**·e2e **172**(pg 5438, migrate deploy)·web build 0(24라우트, 격리 사본)·flutter analyze 0·flutter test **114**. 마이그레이션 additive(단일 ADD COLUMN) 확인. **세액 3중 대조(독립 손계산+순수함수+실서버 curl 라이브 시드) 일치**: 15만 이하 0·**소액부징수 경계 crisp**(일급 187,000→실세액 999→**0**, 187,100→1,001.7→**1,000**, `floor10(x)<1000⇔x<1000` 무결)·다일수 20만×3일=4,050·**3.3% 원단위(10원) 절사 소득세·지방세 양쪽 floor10 일관**(374,100→11,220/1,120). **정합**: site-costs 총계=확인서 합(1,274,100)·wage 지급총액=payments 합(974,100)·팀 파생 ledger(businessId=null·conf관계 없음) 이중배제. **엣지 라이브**: >12개월 400 RANGE_TOO_LONG·정확 12개월 200·from>to 400·잘못된 월 400·0-data 3화면 빈안내+notes 유지·KST today 정확·출역 상태 3종(SCHEDULED/ACCEPTED/STARTED cond) 매핑·**마감 후 신규지급 표시(데이터 안 얼림)**·스코프 격리(outsider·foreign biz→0, biz2→30만만)·미인증 401·PDF 15현장→3페이지 페이지브레이크(%PDF inline 마스킹). 면책 notes 5줄+주민번호 비수집 app·web 양쪽 렌더 확인. **관찰 5건(LOW·백로그)**: 마감이 스냅샷 아님(마감 후 추가분 "마감됨" 배지 합산—경고 권고)·미가입 수기 작업자 출역 미표시(Job.profileId 필수, 스펙 준수)·부분입금 다월 분할 일급 왜곡(문서화 한계)·PDF 소계 동액 현장 삽입순 정렬(cosmetic)·P5c worklog 포트 5437/3050 기재(금지포트 아님·무손상). **live-db(5432)·사용자 스택(5439/3010/3001) 무손상 실측**(3010 health ok·3001 200·5439/5432 Up), git 무변경, 임시 pg 5438·백엔드 3060 정리.
- 2026-07-17: **P5c 사업장 강화 3종 웹 화면 (web)** (`worklog/2026-07-17-P5c-웹.md`) — web/ 만(backend·app 무수정, P5a API 소비). "작업온" 사업장 웹 `/biz/*` 에 사이드바 메뉴 3개(출역 현황·현장 인건비·지급명세서) 추가. **① 출역 현황판** — `/biz/inbox`(홈) 상단 오늘 출역 요약 카드(4지표) + `/biz/attendance` 상세(현장별 표: 작업자·상태 배지[예정/수락/작업중/완료]·시작~종료·컨디션, **30초 자동 갱신**). **② `/biz/site-costs`** — 월 범위 선택(from>to·12개월 초과 사전 차단) → 현장별 **접이식 표**(작업자 연인원/공수/금액·팀 N명 배지·소계)·전체 총계·**PDF 다운로드**(authedBlob, 발주처 제출용). **③ `/biz/wage-statement`** — 월 선택 → 작업자별 표 + **소득유형 토글**(3.3%↔일용, 세액·차인지급액 재계산)·홈택스 **복사** 버튼·**월 마감**(멱등·`마감됨` 배지)·**안내 노트 필수 표시**(주민번호 비수집 경고·"세무상담 아님"). 전 조회 **BizContext businessId 스코프**(BusinessSwitcher 전환 전파)·서버 마스킹본·데스크톱+모바일 반응형(`.data-table` 가로 스크롤). **검증**: 격리 사본 `npm run build` 타입에러 0(21라우트), 임시 스택(pg **5437**/api **3050**/web **3005**) 시드(2사업장·SIGNED 확인서·지급 117만·오늘 jobs 4건) playwright 실측 — 3화면 렌더·site-costs 총계 144만/PDF %PDF 19KB·wage 3.3%↔일용 토글(김\*수 18,000↔4,050)·복사 페이로드·6월 마감·**제2사업장 스코프 전환 30만만**·모바일 390, 스크린샷 9장(`web/screenshots-p5c/`) Read 눈검수 수치 대조·콘솔 error 0. **사용자 web(3001) 핫리로드로 신규 라우트 200 반영(dev 재시작 없음)**, 임시 스택(5437/3050/3005) 정리, live-db(5432)·사용자 스택(5439/3010/3001)·타 에이전트 pg(5438) 무손상.
- 2026-07-18: **웹 디자인 개선 P2 (web)** (`worklog/2026-07-18-웹개선.md`) — **web/ 만 수정**(backend·app 무수정, 기존 공개 API 소비). 감사 5.2/10 지적 P2 대응. **① 종이 확인서 서명**: 초록 텍스트 배지 → **도장(인영) 박스**(붉은 인주색 원형 인영·서명자명 각인+큰 이름+"서명 완료"+일시, 6언어 `paperSignConfirmed` 신설). ★단 **손글씨 서명 이미지 실렌더는 공개 API가 이미지 미노출로 불가 → 백엔드 요청사항 명시**(`GET /public/confirmations/:token` 에 `signImageUrl`/base64 추가 필요). **② 다국어 금액**: `money()` 로케일 그룹핑(ru 공백·vi 점) → **항상 ₩+한국식 콤마**(`won` 일원화, `grouped` 폐기) — vi `₩1.050.000`→`₩1,050,000`, ru 공백→콤마, 6언어 확인. **③ QR 명함(/p)**: 제네릭 → **종이 명함 메타포**(오렌지 헤더·큰 이름·업종 역할·**서류 유효 인증 스트립 강조**·서류/장비 칩). **랜딩(/)**: 2×2 카드·하단 공백 → **수직중앙 한 문장+큰 CTA 1개**+절제된 칩. **④ biz 정리**: `.biz-main` 1000→880px(행 늘어짐 완화)·표/행 hover. **D-day 앱 규칙 통일**: `ddayBadge`(D±) 폐기 → `expiryBadge`(만료 지남="만료됨" 빨강/≤30 "D-N"/>90 생략)·`collectBadge`(기한 지남/"수금 D-N"), `/w/home·documents·ledger·/s`(`docExpired` 6언어) 적용. **⑤ 영문 enum**: 웹은 이미 전면 한국어 라벨(신규 노출 0). **검증**: 격리 사본 `npm run build` **0**(23라우트, 사용자 `web/.next` 무접촉), 임시 스택(pg **5437**/api **3050**/web **3006**) API 시드(연결·서명 확인서·서류유효·명함) playwright 전후 스크린샷 **10장**(`web/screenshots-webimprove/`, 데스크톱1280+모바일390) Read 눈검수(도장박스·₩콤마 ko/vi/ne·명함 유효스트립·랜딩·biz 정리·만료됨/기한 지남). 자원 정리(3006/3050 PID 종료·pg 5437 삭제), **사용자 web(3001) 200·api(3010) 200·live-db(5432)·5439·타 에이전트 pg(5440) 무손상 실측**. **미완**: 서명 이미지 실렌더(백엔드 요청 대기).
- 2026-07-18: **확인서 손글씨 서명 이미지 실렌더 (backend+web, app 무수정)** (`worklog/2026-07-18-웹개선.md` "서명 이미지 실렌더" 추기) — 위 P2 미완(백엔드 요청사항) 해소. **백엔드 additive**: `confirmations.service`(`publicView`·`bizConfirmationDetail`)에 `signImageDataUrl`(SIGNED 만), `labor-contracts.service`(`publicView`)에 양측 `employerSignImageDataUrl`·`workerSignImageDataUrl`(각 서명 완료 쪽만) — base64 PNG data URI, PDF 임베드 `readFile` 재사용, `loadSignDataUrl` 헬퍼(실패 null), 기존 필드 무변경. **웹**: `PaperConfirmation`·`PaperLaborContract` 도장 박스 자리에 실제 손글씨 획 `<img>` 렌더(서명자명·"서명 완료"·일시 유지, 없으면 `.seal` 인영 폴백), `globals.css .sign-stroke`(흰 종이 바탕→라이트/다크 획 가시). biz 수신함 상세는 동일 컴포넌트로 자동 반영. **검증**: e2e 추가(미서명 부재+SIGNED `data:image/png;base64,` 포함, labor 양측) → **전체 e2e 174**(임시 pg **5438**, 172+2)·백엔드 타입체크 0·**web build 0**(격리 사본, 사용자 `web/.next` 무접촉, 24라우트). **playwright 실측**(pg 5438·api **3060**·web **3006**, 손글씨 곡선 PNG 시드)→ `/c/{token}` 스크린샷 `web/screenshots-webimprove/after-c-signed-signature-ko.png` Read — 파란 손글씨 획 실렌더 확인. 자원 정리(3006/3060 종료·pg 5438 삭제·시드 스크립트 삭제), **사용자 web(3001)·api(3010)·live-db(5432)·5439·타 에이전트 pg(5440) 무손상 실측**.

- 2026-07-18: **P1 앱 UX 개선 + 작업자 상세 서명 획 + 디자인 재감사 5.2→7.9** (`worklog/2026-07-18-앱UX개선.md`, `worklog/2026-07-18-디자인재감사.md`) — **① 앱 P1 8건**(DESIGN-IMPROVE P1 전체): (SIGNED) 등 영문 enum 제거(6언어)·지갑 "선택해서 보내기" 상시 버튼+선택모드·장부 주 행동 1개(입금 기록)+⋯ "이 건 관리" 시트·`ddayUnified()` D-day 통일(D-N/D-day/+N일)·아이콘 아웃라인 수렴·홈 상시 "확인서 쓰기" 대형 CTA·정산 상단 합계 카드·`SignatureSeal` 도장 박스(획 이미지 실렌더+인영 폴백). 신규 5키×6 ARB. **② 작업자 인증 상세 서명 획**(backend `getOne`에 `signImageDataUrl` additive(SIGNED만·목록 제외)+앱 모델·상세 전달) — 서명 획이 **앱 작업자/biz·웹 /c·PDF 전부 실렌더**. **③ 버그 2건 수정**: 확인서 상세 "전화 걸기" 무한 너비 크래시(Expanded 누락)·서류 만료 상태 `EXPIRING` vs `EXPIRING_SOON` 불일치(D-5가 초록 표시). **검증**: analyze 0·flutter test **115**·backend confirmations 스펙 13·curl 실측(상세 data:image/png 존재·목록 무), **after 스크린샷 6장 Read 눈검수 합격**(1차 3장 불합격[도커 다운 상태 캡처] → 캡처 테스트 재작성[로드 대기+오류화면 assert] 후 재캡처, 오케스트레이터 교차 확인). 캡처 스택 3070/5440(workon-uxfix-pg)만 사용, 사용자 스택(3010/3001/5439)·live-db(5432) 무접촉. **④ 독립 재감사**(구현과 분리, 동일 축+신설 "한눈에 위계"): **총점 5.2 → 7.9/10** — 원 Top10 중 해결 6(★1 PDF 글리프·★2 서명·★3 enum·★4 묶음·★6 장부·★8 금액)·부분 3(★5 죽은공간·★7 랜딩·★9 D-day 홈 문구 중복)·미해결 1(★10 아이콘 통일). 남은 Top: **홈 "내 돈" 단일 히어로·캘린더 장부화**(사용자 피드백 "토스처럼 한눈에"와 동일 방향, 다음 배치), PDF 5종 렌더 회귀·biz 웹/명함 시각 확인용 시드 보강.

- 2026-07-18: **홈·캘린더 위계 배치 (토스식 "한눈에") + 데스크톱 랜딩** (`worklog/2026-07-18-홈캘린더-위계배치.md`) — 재감사 잔여 Top·사용자 피드백 대응. **앱(app/)**: ① 홈 최상단 **"이번 달 받을 돈" 단일 대형 히어로**(40px 미수색)+보조 스탯(받은 돈·일한 날), 기존 2분할 요약 카드 흡수·제거 → 인사→히어로→CTA→오늘 일정→만료 배너 ② **캘린더 장부화** — 날짜 탭 시 그리드 아래 인라인 장부 펼침(현장·상대·시간·상태 배지·큰 금액, 재탭 접기, 탭→상세), 월 상단 "받을 돈" 히어로 동일 서식 ③ 홈 만료 문구 중복 수정("자격증 만료 만료됨"→"자격증 만료됨"/"만료 D-7"). ARB 신규 4키·변경 1키×6언어. **웹(web/)**: 데스크톱(≥1024px) 랜딩 2단 히어로 — 좌 카피+CTA(기존 유지)·우 **실제 앱 스크린샷 폰 목업 2장**(외부 다운로드 없음)+신뢰 3블록, 모바일 무회귀·다크 대응. **검증**: analyze 0·flutter test 115·신규 uxfix2 캡처 4테스트 통과, 스크린샷 3장(uxfix-07~09)+랜딩 3장(데스크톱/모바일/다크) **구현·오케스트레이터 양쪽 눈검수 합격**, 격리 사본 web build 0(사용자 .next 무접촉). 에이전트 보고 "캘린더 +1일" 의심은 **버그 아님 확정**(DB UTC 저장의 정확한 KST 변환, psql 원시값 오해). 캡처 스택 3070/5440만 사용, 사용자 스택·live-db 무접촉.

- 2026-07-18: **캘린더 미수/입금 분리 + 구글식 일감 미니 라인 (backend+app, web 무수정)** (`worklog/2026-07-18-캘린더-미수입금분리.md`) — 캘린더가 청구(billed)만 보이던 것을 정산 상태 반영으로 확장. **backend additive**: `GET /confirmations?month=` 각 확인서에 연결 ledger entry 기준 `settlement:{paidAmount,outstandingAmount,status(UNPAID|PARTIAL|PAID)}` + top/byDate 에 `totalPaid`/`totalOutstanding`(billed 무변경). 순수함수 `settlement.util`(파생 팀원 이중배제=소유자 목록 특성상 자연 배제). ledger entry 는 DRAFT 생성 시점부터 존재 → 캘린더 미수/입금 총계가 **홈 히어로(ledger summary)와 수치 정확 일치**(curl 실측 paid 1,090,000/out 1,280,000/billed 2,370,000 3필드 일치). **app**: 모델 `Settlement`·`totalPaid/Outstanding` 파싱, **날짜 칸=구글 캘린더 월뷰식 일감 미니 라인**(최대 3줄, 현장명 말줄임+상태 칩색: 입금완료 초록/미수 주황/초안·전송 회색, 초과분 +N, `childAspectRatio 0.60`), 날짜 탭 패널은 그날 **전건** 리스트(현장·상대·금액·상태, PARTIAL '입금 N원' 보조줄), 월 히어로 "받을 돈"=미수합. ARB 신규 0(기존 `ledgerDeposited` 재사용). 홈 오늘 일정은 이미 다건 렌더(확인만). **검증**: nest build 0·단위 **192**(+6)·e2e **175**(신규 정합 테스트 포함, runInBand)·flutter analyze 0·flutter test **119**(+4)·스크린샷 2장(uxfix-12 그리드 3줄 혼재·uxfix-13 3건 펼침+부분입금) **눈검수 합격**. 검증 3070 재빌드·PID 재기동, e2e 임시 pg 5442 후 삭제, 별도 시뮬 `workon-uxfix12b` 사용 후 삭제. 사용자 스택(3010/3001/5439)·live-db(5432) 무접촉. **참고**: 팀원 파생소득은 소유 확인서 없어 캘린더 미표시(백로그); `iPhone 17 workon` 은 UDID 무명령이나 세션 중 Booted→Shutdown 관찰(원인 미상, 재부팅 안 함).

- 2026-07-18: **거래처(Partners) 1단계 — 자동 수집 + 문자/전화 바로가기 (backend + app, web 무수정)** (`worklog/2026-07-18-거래처-1단계.md`) — 확인서·장부의 수기 상대를 **자동 수집**해 목록 제공(등록 작업 0). **backend additive**(마이그레이션 `20260718120000_partners`): `partners` 테이블(id·profileId·name·phone?·alias?·bizNumber?·email?·memo?), **식별 키 (profileId, name)**(장부 by-company/확인서 집계가 name 만으로 그룹핑 → phone 은 name 하위 가변 속성; (name,phone) 키는 통계 파편화로 기각, 주석 명시). **통계는 저장 안 함** — 건수·미수·최근일은 조회 시 confirmations/ledger 파생. **병합 채택(백필 불필요)**: `GET /partners` 가 확인서+장부+보강행+연결사업장을 병합, 확인서에만 있고 partner 행 없는 수기 상대는 조회 시 **lazy 생성**(createMany skipDuplicates). 확인서 create/update/duplicate 에 **자동 upsert 훅**(try/catch, 확인서 저장 무영향). API: `GET /partners`(연결 상대 businessId·linked 배지 포함, 미수는 derived 제외), `PATCH /partners/:id`(alias/bizNumber/email/memo 만, whitelist), `DELETE`(hard, lazy 재수집). **app**: 더보기→거래처 목록(검색·미수·연결 배지·정산완료)·상세(요약+액션 3버튼[문자→빠른보내기 프리필/전화/확인서 쓰기]+보강 편집), **빠른 보내기 수신인 시트**에 "거래처에서 선택"(칩)·"기기 연락처에서 선택"(**시스템 피커 CNContactPicker/ACTION_PICK, MethodChannel `kr.workon/contacts`, 무권한** — READ_CONTACTS·NSContactsUsageDescription 미추가), 확인서 폼 "거래처에서 선택" 버튼. 신규 ARB 키(827×6 패리티 0결손). **검증**: 백엔드 build 0·단위 **197**(+5)·e2e **182**(+7: 자동수집·병합/통계·PATCH 필드제한·연결상대·권한격리·hard delete+lazy 재수집, 임시 pg 5442 삭제)·**curl 실측**(부분입금 후 partners outstanding 300,000 == ledger by-company 정합, PATCH whitelist). app **analyze 0·test 122**(+Partner.fromJson)·**iOS·Android 빌드 통과**(iOS 오케스트레이터 재빌드 확인)·**스크린샷 3장 독립 눈검수 합격**(목록 4상대·상세 액션3+별칭 편집·수신인 시트 거래처칩/연락처버튼). 연락처 피커 실팝업은 시뮬 out-of-process 캡처 불가 → 코드경로+양 플랫폼 컴파일로 확인(정직 보고). 검증 3070 재빌드·PID 재기동, 사용자 스택(3010/3001/5439)·live-db(5432)·기존 시뮬 무접촉. **미해결**: 피커 실팝업 수동 QA; 2단계(배포 시) 팝빌/바로빌 세금계산서 발행 어댑터.

## 진행 중
- **S1~S5 전 단계 완료 + P1 전 항목 완료.** 백엔드+웹+앱 구현·검수 + 통합 스택 E2E 검증 + 배포 산출물 완비. **배포 준비 완료** — 실제 서버 배포는 `docs/DEPLOY.md` 절차를 따른다.
- **P1 완료 항목**: 공수(품) 단위, 오프라인 대응, 세금계산서 1단계, 카카오 로그인(UI 조건부·네이티브 키 대기), 다국어(웹+앱 6언어), **홈 화면 위젯**.
- **P1 통합 검수 완료 + 지적결함 1건 수정 완료(`worklog/2026-07-11-P1-통합검수.md`)** — 배치 경계·회귀 검수. 회귀 전부 재현(백엔드 단위 87·e2e 78·web build 0·flutter analyze 0·flutter test 58), 세금계산서 집계 수동대조 일치, 알림톡 비활성 로그 3경로, i18n 키패리티(웹 56→57키×6·앱 422×6 결손0)·웹앱 공통문구 27/28 동일, live-db(5432) 무손상. **판정: 수정 필요 1건 → 수정 완료**.
  - **[중요·✅ 수정 완료] 웹 외부 열람 공수 단위 미표시(비한국어)**: `web/components/PaperConfirmation.tsx` 수량 span 에 `it.unit` 표기 추가(6개 사전에 `unitGongsu` 키 신설, 앱 ARB 일치: zh `工数`·ru `гонсу`·en/vi/ne `gongsu`). 비한국어는 `번역단위 + 한국어 '공수' 병기`(예 ne `× 1.5 gongsu (공수)`)로 서명자가 한국 원본과 대조 가능, 한국어는 서버 라벨 `기본(공수)` 유지로 무회귀. **검증**: `next build` 타입에러 0, 임시스택(pg 5436·백엔드 3040·web 3004) 공수 확인서 시드→ko/ne/ru/vi 4언어 playwright 실측(단위·금액 270,000 정확, 한국어 회귀 없음). 스크린샷 `.playwright-mcp/gongsu-{ko,ne,ru,vi}.png`.
  - **[사소]** 미사용 앱 ARB 키 12개, 초안 큐 flush중 신규초안 지연(데이터 손실 아님). **[수동 QA]** iOS/Android 위젯 홈배치·다크·탭, 실기기 오프라인 토글, 원어민 번역 검수.
- 전권 위임 모드: 구현→검수→수정 자동 루프 (CLAUDE.md §5.5)
- **로컬 테스트 환경 (2026-07-18 갱신)**: API 3010 최신 빌드 재기동(signImageDataUrl 반영)·web 3001·pg 5439. **데모 시드**(API로만 주입): 계정 010-7777-0001("이작업"), 서류 4종(만료/D-5/유효2), 확인서 3건(7/11 판교 220,000 SIGNED[손글씨 획]·7/14 역삼 180,000 SENT·오늘 성수 250,000 DRAFT), 장부 미수 650,000. 새 디자인 앱은 시뮬 "iPhone 17 workon"에 설치·부팅 상태(BASE_URL=3010). dev 인증코드는 `/tmp/workon-api.log`의 `[MOCK SMS]` 또는 응답 devCode. 캡처 스택(3070/5440)도 기동 중.

## 완료 (개발)
- ✅ S1 기반 골격 — 구현+독립 검수 통과 (`worklog/2026-07-11-S1-기반골격.md`). 통합 Docker 스택 실기동 검증 완료
- ✅ SKEP 진위확인 분석 (`worklog/2026-07-11-진위확인-분석.md`) — 사업자등록 API 우선, 면허는 수동확인 fallback
- ✅ S2a 인증·공통 인프라 (`worklog/2026-07-11-S2a-인증.md`) — 봉투/가드/OTP/JWT, 단위 9·e2e 3
- ✅ S2b 서류·장비 (`worklog/2026-07-11-S2b-서류장비.md`) — 장비 CRUD, 서류 업로드(PDF 정규화)·마스킹·진위확인·묶음 공유(public)·만료 크론. 단위 37·e2e 13, 실서버 curl 검증 완료. 마이그레이션 `20260711020000_s2b_documents_equipment`
- ✅ S2c 확인서·장부 (`worklog/2026-07-11-S2c-확인서장부.md`) — 작업확인서(amountCalc 서버계산+ledger 자동생성, DRAFT 수정/삭제·ledger 동기화, duplicate, month 캘린더 집계, send, public 열람/서명 409, 확인서 PDF+한글폰트 임베드+서명이미지), 장부(summary/by-company 상태·D-day, 부분입금, 명세서 PDF, 수금 D-day 크론). 단위 55·e2e 16(+회귀 documents13·auth3), 실서버 curl+pdftotext/pdffonts 검증 완료. 마이그레이션 `20260711030000_s2c_confirmations_ledger`. 의존성 `@pdf-lib/fontkit` + `assets/fonts` 한글 폰트
- ✅ S2d 연동·작업지시·사업장·안전·알림 (`worklog/2026-07-11-S2d-연동사업장안전.md`) — 사업장/초대코드/작업자검색(마스킹)/연결·미가입 승격, 작업지시(confirm 서류확인·start 컨디션·complete·사진 multipart), biz(inbox/앱내서명/정산 pay 양측일치/안전리포트 PDF), 폭염 자동알림(WeatherService·HeatwaveScheduler 06:00/14:00 KST·ack 1회 409), FCM 실발송(firebase-admin, 키없으면 로그만)·알림 조회/읽음/device-token. 크론 전체 Asia/Seoul. **폰트 라이선스 교체**(AppleGothic→정품 OFL NanumGothic). 단위 71·e2e 19(전체 48/48), 실서버 curl+PDF 검증 완료. 마이그레이션 `20260711040000_s2d_connections_safety`. 의존성 `firebase-admin`. infra compose healthcheck+TZ 이월 처리
- ✅ S2 통합 검수 수정 (`worklog/2026-07-11-S2-통합검수.md`) — [중요] 확인서 상대 businessId ACCEPTED 연결 검증(create/duplicate/PATCH, bizSign SENT 강제) + [사소6] viewLogs cap50·viewCount, 서명 TOCTOU 원자화, 정산 pay 트랜잭션, job 상태전이 강제, manualContact 인덱스, 사진 MIME 검증. 단위 73·e2e 55 통과, 실서버 curl 재현 차단 확인. 마이그레이션 `20260711050000_s2_review_hardening`. 백로그: 승격 전역 스캔 DB측 필터 최적화
- ✅ S3 웹 (Next.js 15) (`worklog/2026-07-11-S3-웹.md`) — 외부 공개(`/c/[token]` 종이 확인서 SSR·OG·캔버스 서명→SIGNED·가입유도, `/s/[token]` 서류 묶음) + 사업장 웹(`/login` 전화인증·JWT·401 리다이렉트, 사이드바·사업장 생성 유도, inbox 앱내서명, settlements pay 작업자장부 대칭, workers 검색·연결·작업지시, safety 인증 blob PDF) + `/` 랜딩(S1 지적 해소). 디자인 토큰 CSS 변수(오렌지/네이비/종이톤·다크모드)·PaperConfirmation·SignaturePad(캔버스 직접구현) 재사용. axios 인터셉터 봉투 언래핑. **playwright 실검증**(모바일 375·데스크톱 1280, 서명·정산 대칭·인증 PDF·리다이렉트·다크모드) + `npm run build` 타입에러 0. 임시 pg(5433)·서버 정리, live-db(5432) 무손상. 의존성 `axios`
- ✅ S3 독립 검수 지적 수정 (`worklog/2026-07-11-S3-웹.md` 하단 추가) — [중요] SignaturePad 리사이즈 서명 보존(벡터 리드로우), 공개 SSR 8초 타임아웃+친화 화면/`notFound()` 404+`error.tsx`, 수신함 모달 상세 엔드포인트(`GET /biz/confirmations/:id`)로 시간·금액 완전 렌더, `/s` 다운로드 `?download=1`→attachment. [사소] 모바일 topbar 사업장명+로그아웃, 파비콘(오렌지+흰 온 SVG), 13→14px, 로그인 코드 6자리 고정. 재검증: 단위 73·e2e 55·web build 0, playwright ①~⑤ 실측 통과. 임시 pg 5434·백엔드 3020·web 3002 정리, live-db(5432) 무손상. 백로그: **사업장 다중 소유 전환 웹 UI**(백엔드는 대응됨)
- ✅ 루트 .gitignore 생성 (보안 지적 즉시 조치)
- ✅ S4a Flutter 앱 코어 (`worklog/2026-07-11-S4a-앱코어.md`) — 작업자 관점 전 화면: 로그인/온보딩(전화인증·devCode 자동), 홈(오늘 일정 종이카드·이번 달 요약 미수/입금), 캘린더(월 그리드 도트+금액 ↔ 주간 리스트 토글), 확인서 작성(이전복사·연결/수기 상대·단가유형·연장야간·장비토글·실시간 금액계산·저장→share_plus 공유시트), 장부(회사별 D-day 4상태·회사상세 항목별 부분입금·명세서 PDF 인증blob), 더보기(프로필·S4b 자리). 디자인 토큰 ThemeExtension(라이트/다크)·종이 확인서 PaperCard·tabular figures·시스템폰트. dio 봉투언래핑+JWT secure storage+401. **flutter analyze 0 errors, 단위/위젯 16 통과, iOS 26.5 시뮬레이터 E2E(flutter drive) All passed + 스크린샷 8장 눈검수, 장부 반영 curl 대조**. 임시 pg(5435)·백엔드(3030) 정리, live-db(5432) 무손상. **백엔드 추가**: `GET /ledger/entries?month=`(앱 부분입금용 항목 id, 최소·비파괴, ledger 단위 12/12 통과). 의존성 riverpod/go_router/dio/flutter_secure_storage/share_plus/path_provider/open_filex/intl
- ✅ S4b Flutter 서류 지갑·사업장 모드·알림 (`worklog/2026-07-11-S4b-지갑사업장.md`) — 서류 지갑(그리드·만료 D-day·업로드 image_picker/file_picker·마스킹 편집기 드래그→정규화좌표·상세 인증blob 미리보기·묶음 공유 7/14/30일→시스템 공유시트·내 공유 열람/무효화·장비 CRUD), 사업장 모드(hasBusiness 분기 생성/홈·수신함 PaperCard 상세·**앱내 캔버스 SignaturePad 서명 SIGNED**·정산 pay 작업자장부 대칭·작업자 검색/연결/작업지시·안전 리포트 PDF), 작업자 받은작업(수락·시작 GPS+컨디션·완료), 알림(목록·읽음·폭염 ack·홈 벨 뱃지·FCM 구조 설정없으면 skip·전화검색 동의 토글). **flutter analyze 0 errors/0 warnings, 단위/위젯 27 통과, iOS 26.5 시뮬레이터 integration_test All passed + 스크린샷 14장 눈검수, 백엔드 curl 대조(DONE/SIGNED/paid 대칭)**. 임시 pg(5435)·백엔드(3030) 정리, live-db(5432) 무손상. **백엔드 추가**: `GET /documents/:id/file`(인증 미리보기, 최소·비파괴). 의존성 image_picker/file_picker/geolocator/firebase_core/firebase_messaging. iOS 배포타깃 15.0 상향
- ✅ S4 독립 검수 지적 수정 (`worklog/2026-07-11-S4-검수수정.md`) — [중요] 공통 `ErrorRetry` 위젯(친화 메시지 "연결에 문제가 있어요"+"다시 시도" provider invalidate) 홈/캘린더/장부/회사상세/지갑/알림/사업장/작업자/장비/수신함 에러 표시부 일괄 적용, 장부 빈 상태 CTA(PaperCard 안내 "확인서를 작성하면 장부가 자동으로 채워져요"+확인서 작성 버튼, 홈 스타일 통일). [사소] dispose 누락(biz 수신함 상세 `_sig`/`_nameCtl`, 사업장 생성, 작업자/작업지시 폼)+바텀시트 임시 컨트롤러 해제(whenComplete/try-finally), 확인서 수량>0 검증(0이면 저장 비활성+안내), use_build_context_synchronously 2건·pop-후-SnackBar 2건 정리(메신저/네비게이터 사전 캡처), 서명 1MB 검사를 디코드 PNG 바이트 기준으로 백엔드(`decodeSignPng`)와 일치. **flutter analyze 0 issues(기존 info 10→0), 단위/위젯 30 통과(신규 3)**, iOS 26.5 시뮬레이터 E2E(임시 pg 5435/api 3030): 빈 상태 CTA·백엔드 다운→친화 에러+재시도→재기동 복구 스크린샷 검증. 임시 자원 정리, live-db(5432) 무손상
- ✅ S5 통합 검증·배포 준비 (`worklog/2026-07-11-S5-통합배포.md`) — 백로그 e2e 보강(documents/:id/file·ledger/entries, 권한격리 → **e2e 55→71**, 단위 73). **운영 구성 통합 스택**(compose 빌드·기동, `ports: !override` 18081 격리, NODE_ENV=production) nginx 경유 시나리오 ①~⑤ 전부 통과(curl+playwright): 가입→서류(이미지→PDF)→마스킹→공유→외부 웹열람, 확인서→웹 서명 SIGNED→장부 자동, 같은전화 사업장 자동승격→정산→작업자 PAID 대칭, 작업지시~완료, prod 안전장치(devCode 미노출·dev엔드포인트 403·약한 JWT fail-fast). **발견·수정 3건**: 웹 API베이스(브라우저 상대`/api`+SSR `API_INTERNAL_URL` 분리), Next standalone `HOSTNAME=0.0.0.0` 헬스체크, 공개링크 env(PUBLIC_WEB_URL 등) 누락 → 재빌드 재검증 통과. **배포 산출물**: `docker-compose.prod.yml`(재시작·로그로테·리소스제한·비루트 api uid1001), `deploy.sh`·`backup.sh`, **`docs/DEPLOY.md`**(한국어 체크리스트+외부키 4종 발급 절차), `.env.example`·README 갱신. 테스트 자원 전부 정리(컨테이너·볼륨·이미지), live-db(5432) 무손상(임시 pg 5436·nginx 18081만 사용).
- ✅ P1 백엔드 (`worklog/2026-07-11-P1-백엔드.md`) — **공수(GONGSU) 단가유형**(소수 0.1단위 검증·PDF "1.5공수 × 180,000" 라벨·ledger summary `totalGongsu`), **세금계산서 1단계**(`GET /ledger/tax-invoice-data` JSON+복사텍스트, SIGNED·미발행만 상대별 집계·공급가/세액10%, `POST .../mark`로 `taxInvoicedAt` 표시 후 제외, 공급자 `profiles.bizNumber/bizName/bizAddress` PATCH /me 등록), **알림톡 어댑터**(`AlimtalkService`+`SolapiAdapter` 실호출 코드·키없으면 로그만, 확인서 send 수기상대·수금 D-day·폭염에 채널확장, 알림톡=푸시 미도달 상대 우선, 템플릿 문구 상수·카카오 승인 주석), **카카오 마무리**(`POST /auth/kakao/link` 기존 전화계정에 kakaoId 연결·409 충돌·501 스텁). 마이그레이션 `20260711060000_p1_gongsu_tax_alimtalk`. **단위 87·e2e 78 통과(신규 단위14·e2e7), lint 0 error, build OK, 실서버 curl+pdftotext 검증**. 임시 pg 5436·백엔드 3040 정리, live-db(5432) 무손상. 의존성 추가 없음.
- ✅ P1 다국어 — 외부 공개 웹 (`worklog/2026-07-11-P1-다국어-웹.md`) — **web/ 외부 페이지만** 6개 언어(한국어 기본 + 중국어 간체·러시아어·베트남어·네팔어·영어). 경량 사전(dictionary) 방식(`web/lib/i18n/{ko,zh,ru,vi,ne,en}.ts` + `t()`/`createT()`, `ko` 기준 `MessageKey` 완전성 컴파일 강제, SSR용 `server.ts`). 언어전환 UI(지구본+자국어 드롭다운, 쿠키+localStorage+`?lang=`, `router.refresh` SSR 전환), 초기언어 `?lang→쿠키→Accept-Language→ko`. `/c`(종이 확인서·서명·완료·가입배너)·`/s`(서류묶음)·404/오류(StatusScreen)·서명패드 힌트 번역. 데이터(현장/회사/작업내용/서명자명)·OG 태그는 미번역. 금액/날짜 Intl 로케일(통화 원화 고정 ko `원`/그외 `₩`), 데바나가리 폰트 폴백·동적 `<html lang>`. 공유 컴포넌트(PaperConfirmation/SignaturePad)는 lang 기본 ko → 사업장 웹 무변경. **검증**: `npm run build` 타입에러 0, playwright(모바일) 6개 언어 `/c` 스크린샷 눈검수(네팔어·러시아어 글자 깨짐 없음)·`?lang=vi` 직접진입·새로고침 언어유지·베트남어 서명 제출 성공(백엔드 signed 대조)·`/s`(en/ne)·404(ne/ru) 확인. 임시 pg 5437·백엔드 3050·web 3004 정리, live-db(5432) 무손상. **미해결**: 원어민 검수 권장(법률 문구·직역 용어). 스크린샷 repo 루트 `i18n-*.png`.
- ✅ P1 다국어 — 앱(Flutter) (`worklog/2026-07-11-P1-다국어-앱.md`) — **app/ 전 화면** 6개 언어(한국어 기본 + 중국어 간체·러시아어·베트남어·네팔어·영어). **flutter_localizations+intl ARB**(`l10n.yaml`+`lib/l10n/app_{ko,zh,ru,vi,ne,en}.arb`, ko 템플릿 **417키×6언어** 패리티 검증, `context.l`/`context.lang` 확장). 설정(더보기) **언어 선택**(시스템 따름+6언어 자국어 표기, shared_preferences 저장, 기본 시스템 로케일→미지원 ko). 날짜·금액 웹 규칙 동일(`formatMoney` ko `원`/그외 `₩`, Intl 로케일 요일·월, ne 데바나가리 숫자+라크·ru 공백 천단위). 전 화면 추출(로그인/온보딩·홈·캘린더·확인서 작성(공수·초안)·장부·세금계산서·서류지갑·마스킹·공유·사업장 모드 전체·받은작업·알림·설정·ErrorRetry·D-day 라벨·단가유형). 금액 항목 type 기준 번역·OTHER 라벨 데이터 유지, **홈택스 복사 텍스트 한국어 고정(정책)**·확인서 PDF(백엔드)·데이터(현장/회사/이름/서류유형 값)·브랜드 미번역. 긴 번역 오버플로 수정(장부 헤더·폼 칩 Wrap·금액/시간 FittedBox scaleDown). **검증**: gen-l10n 정상, `flutter analyze` 0, `flutter test` 54 통과(기존 53 회귀 — 문자열 의존 테스트 l10n 방식 수정), **iOS 시뮬 integration_test All passed**(임시 pg 5436/백엔드 3040): 언어 전환 vi/ru/ne 실동작 + **스크린샷 10장 눈검수**(`app/screenshots/i18n-*.png`, 글자 깨짐·오버플로 0 — 1차 실측서 ru 오버플로 2건·금액 말줄임 발견→수정 재검증) + **베트남어 확인서 작성·저장 성공**(성조 원문 저장·SENT 백엔드 대조). 자원 정리, live-db(5432) 무손상. **미해결**: 원어민 검수 권장(gongsu·조출 등), ApiException 폴백 메시지 코드 매핑 후속.
- ✅ P1 앱 (Flutter) (`worklog/2026-07-11-P1-앱.md`) — **공수(품) 입력**(단가유형 `공수` 세그먼트·0.5 스텝퍼 0.5~5.0·직접입력 0.1단위 검증·`INVALID_GONGSU_QUANTITY` 대응, 미리보기 `180,000 × 1.5공수 = 270,000`, 홈·장부 월요약 `totalGongsu`·캘린더/확인서 카드 공수 라벨), **오프라인 임시저장**(shared_preferences 초안 큐·네트워크 실패 시 enqueue+스낵바, connectivity_plus 복구 자동 flush·4xx는 초안 유도/5xx·NETWORK 재시도, 홈 대기 배너·초안목록 수동재시도/삭제, 폼 이탈 자동 초안 1건 PopScope 보존/복원), **세금계산서 화면**(더보기→세금계산서 준비, `PATCH /me` 사업자정보, `GET /ledger/tax-invoice-data` 상대별 카드·복사(클립보드)·홈택스 안내·`mark` 발행표시 제외, supplierReady 유도), **카카오 로그인 버튼**(kakao_flutter_sdk_user, `--dart-define KAKAO_APP_KEY` 있을 때만 노출·`POST /auth/kakao`·501 안내, 설정 `kakao/link` 연결·409/501 분기). **flutter analyze 0 issues, flutter test 53 통과(신규 23), iOS 시뮬(iPhone 17 Pro) integration_test All passed + 스크린샷 9장 눈검수**(①공수 270,000→저장→장부 1.5공수 ②백엔드 차단 임시저장 배너→복구 자동전송 ③사업자정보→세금계산서 데이터→복사→mark→제외), 백엔드 curl 대조. 의존성 shared_preferences/connectivity_plus/kakao_flutter_sdk_user. 임시 pg 5436·백엔드 3040 정리, live-db(5432) 무손상.
- ✅ P1 홈 화면 위젯 (`worklog/2026-07-11-P1-위젯.md`) — **`home_widget` 기반 iOS(WidgetKit)+Android(AppWidget)**. 오늘 일정(현장·시간/없으면 "오늘 일정 없음")+이번 달 미수금(미수 빨강)·마지막 동기화 시각, 소형(2x2 미수금)·중형(4x2 일정+미수금), 탭→앱 열기, 로그아웃→"로그인해 주세요" 클리어. 문구는 앱 언어 따름(앱이 렌더 문자열을 App Group `group.kr.workon` 공유, 네이티브는 렌더만). Flutter 브리지(`core/home_widget_bridge.dart`, 홈 로드 시 push·중복방지)+l10n 5키×6언어. iOS: `ios/WorkonWidget/`(SwiftUI, 시안 토큰 라이트/다크, containerBackground 17+), WidgetKit 확장 타깃·App Group 엔타이틀먼트·**배포타깃 15.0 통일**(xcodeproj 스크립트)·"Thin Binary" 페이즈 재정렬(사이클 해소). Android: `WorkonWidgetProvider`(HomeWidgetProvider)+RemoteViews 3레이아웃·appwidget-provider·colors(-night)·manifest receiver. **검증**: `flutter analyze` 0, `flutter test` 58, `flutter build apk --debug`·`flutter build ios --simulator`(appex 임베드) 통과. **iOS 시뮬 integration_test All passed** — 앱→App Group 공유 데이터 native 라운드트립 실측(`state=in amount=800,000원 site=대성건설 판교현장 outLabel=이번 달 미수금 synced=오전 11:25 기준`, 백엔드 실대조). 임시 pg 5436·백엔드 3040 정리, live-db(5432) 무손상. **미검증(환경제약)**: iOS 홈 위젯 배치 스크린샷·다크·탭(시뮬 SpringBoard UI 자동화 불가—AX 권한 없음)→통합 검수(GUI)에서 눈검수, Android 실렌더(에뮬레이터 없음→빌드+코드리뷰 대체). 부수: android compileSdk 36 통일(APK 빌드 백로그 해소). 의존성 `home_widget: ^0.9.3`.
- 디자인 v1 피드백 반영 확정: 서류 선택 전송+주민번호 마스킹, 캘린더 월/주 뷰, 사업주 앱 연동(지시·정산·안전알림)

- ✅ **P2a 반장(팀) 기능** (`worklog/2026-07-11-P2a-팀기능.md`) — **백엔드**: `teams`/`team_members` 스키마(마이그레이션 `20260711053017_p2a_teams`, additive) + `/teams` CRUD·팀원 CRUD(반장 본인만, 가입자=전화검색 동의자 연결·수기 허용, 기본 단가), **팀 확인서**(`POST /confirmations` `teamId`+`teamEntries` — 서버가 팀원별 공수×단가·합계 계산(0.1 공수 검증 재사용), rateType=GONGSU, 반장 장부 합계 1건), **팀원 파생 장부**(SIGNED 시점에 가입+연결 팀원에게 각자 몫 `derived=true` ledger 자동생성+알림, 멱등, 읽기전용—입금만 가능 409 LEDGER_DERIVED_READONLY), PDF 팀 명단 표(이름/공수/단가/금액), 공개 API에 `teamEntries`·`isTeam` 포함(웹은 P2 통합 때). **앱**: 더보기→"내 팀"(팀 여러 개·팀원 전화검색/수기·기본 단가), 확인서 작성 "팀 확인서" 토글(팀원별 공수 스텝퍼+단가·실시간 합계), 장부 "팀" 배지·파생 항목 "○○ 반장 팀 작업" 표기, 신규 38키 6개 ARB 전부(460키×6 패리티). **검증**: 백엔드 단위 94(87+7)·e2e 95(78+17, 권한 격리 포함), 실서버 curl+pdftotext 팀 명단 표 확인, flutter analyze 0·test 63(58+5), iOS 시뮬 integration_test All passed+스크린샷 5장(`app/screenshots/p2a-*.png`) 눈검수. 임시 pg 5436/백엔드 3040만 사용·정리, live-db(5432) 무손상.
- ✅ **P2b 표준근로계약서 전자서명** (`worklog/2026-07-11-P2b-근로계약서.md`) — **백엔드**: `labor_contracts` 스키마(마이그레이션 `20260711065300_p2b_labor_contracts`, additive — 사업장FK·작업자(가입 workerProfileId?/수기 이름+전화)·계약기간(시작·종료?)·근무장소·업무내용·근로시간(시업·종업·휴게)·임금(일급/시급+금액)·지급일·방법·주휴/연장 문구 토글·4대보험 JSON·특약·shareToken·양측 서명(이미지+서명자+시각)·DRAFT|SENT|SIGNED·revokedAt·viewLogs/viewCount) + `/biz/contracts` CRUD(소유자만, DRAFT+미서명만 수정)·`sign-employer`(전송 전 필수 선행)·`send`(연결 작업자 알림/수기 알림톡+링크 `/lc/{token}`), `/contracts` 작업자 "내 계약서"(SENT부터 자동 연결)+앱내 서명+PDF, `/public/contracts/:token` 열람(viewLog)·서명(**조건부 updateMany 원자 전이·재서명 409**)·PDF. PDF 조항+**양측 서명 박스**+"정본은 한국어본" 문구(나눔고딕). **웹**: `/lc/[token]` 6언어 열람·서명(PaperLaborContract — 조항 라벨 번역·값 원문·정본 안내, 기존 SignaturePad/StatusScreen/언어 스위처 재사용, i18n 신규 43키×6) + **P2a 이월: PaperConfirmation 팀 명단 표**(이름/공수/단가/금액+팀 합계, 6언어). **앱**: 사업장 메뉴 "표준근로계약서"(목록·작성 폼(전화검색 연결/수기 토글·자주 쓰는 값 저장)·사업주 서명→전송(공유))·작업자 지갑 "내 계약서"(목록·PDF·받은 계약서 서명), 신규 92키×6 ARB(조항 라벨 웹 사전과 일치, 552키 패리티). **검증**: 백엔드 단위 97(94+3)·e2e 111(95+16 — 서명선행 409·재서명 409·권한 격리·지갑 자동연결·수기 링크), 실서버 curl+pdftotext 조항·양측 서명, web build 0·playwright 실측 4장(`web/screenshots-p2b/` — /lc ko 열람·**실서명 SIGNED 왕복**·vi 열람·/c 팀 명단 표), flutter analyze 0·test 70(63+7)·iOS 시뮬 integration_test All passed+스크린샷 5장(`app/screenshots/contract-*.png`) 눈검수. 임시 pg 5436/백엔드 3040/web 3004만 사용·정리, live-db(5432) 무손상.

- ✅ **P2c 간편 TBM(안전점검회의)** (`worklog/2026-07-11-P2c-TBM.md`) — **백엔드**: `tbm_records`/`tbm_attendees`/`tbm_presets` 스키마(마이그레이션 `20260711091745_p2c_tbm`, additive — NotificationType/SafetyLogType `+TBM`, `TbmPresetKind`), `/biz/tbm` CRUD(소유자만·**작성 당일만 수정/삭제**, 이후 읽기전용 409)·사진 업로드/열람(FileStorageService 재사용, 20MB·이미지 MIME)·프리셋 CRUD(HAZARD/MEASURE 커스텀 문구), `/tbm` 받은목록·`POST /tbm/:attendeeId/ack`(**조건부 updateMany 최초 1회 원자 전이·재확인 409**). 생성 시 가입 참석자 알림(TBM) + **safety_logs 에 기록(TBM_RECORD)·확인(TBM_ACK) 각각 append**(businessId 포함). **위험요인 키 기반**([{code|text}] — 기본 프리셋 10종은 코드+한국어 라벨, 커스텀은 원문). `renderSafetyReportPdf` 확장 — **월간 TBM 섹션**(일자·현장·위험요인·참석 N/확인 M). **앱**: 사업장 홈 "TBM 기록"(위험요인 프리셋 칩 다중선택+직접입력+커스텀 프리셋 저장, 연결 작업자 참석자 칩+수기 추가, 조치, 현장 사진, 저장)·목록(참석/확인 배지)·상세(참석자 확인 현황·당일 수정/삭제), 작업자 더보기 "받은 TBM"(내 안전 기록·확인 탭)+알림 TBM 확인 버튼. **위험요인 프리셋을 작업자 자기 언어로**(기본 10 코드×6언어 번역, 커스텀 원문), 신규 64키×6 ARB(616키 패리티). **검증**: 백엔드 단위 103(97+6)·e2e 120(111+9 — 재ack 409·당일 후 수정/삭제 차단·권한 격리·safety_logs 반영·리포트 PDF 200), 실서버 curl+pdftotext TBM 섹션(참석/확인 2/1) 확인, flutter analyze 0·test 74(70+4), iOS 시뮬 integration_test All passed+스크린샷 4장(`app/screenshots/tbm-*.png` — 작성 폼(프리셋 칩+참석자)·저장 목록·작업자 확인 탭·상세 확인 1/1) 눈검수. 임시 pg 5436/백엔드 3040만 사용·정리, live-db(5432) 무손상.

- ✅ **P2d 연간 소득 리포트** (`worklog/2026-07-11-P2d-소득리포트.md`) — **백엔드**(스키마 변경 없음, 기존 ledger 재사용): `GET /ledger/income-report?year=|from=&to=`(월별 추이[데이터 없는 월도 0]·상대별[총액 내림차순]·총계·공수·입금/미수 + **팀 지급분 teamPayout·순소득 참고 netBilled**) + `GET /ledger/income-report/pdf`(인증 blob, 나눔고딕·페이지 브레이크). **집계 순수 함수**(`income-report.util.ts`) — 파생 중복 합산 방지 검증 내장(월별합=상대별합=총계, 파생 항목 teamPayout=0). **팀 파생 원칙**: 팀원 파생은 본인 소득으로 집계, 반장은 팀 확인서 전체가 매출이며 팀원 지급분(본인 몫 제외)을 별도 표기해 순소득 참고 제공(차감 아님). 종소세 일반 안내(5월 신고·3.3% 원천징수·세무상담 아님 명시). 범위 최대 24개월. **앱**: 더보기→"소득 리포트"(연도 선택·총계 카드·**월별 막대 커스텀 페인트**(차트 라이브러리 미사용)·상대별 리스트·종소세 안내 카드·PDF 저장·공유 share_plus), 신규 27키×6 ARB(643키 패리티). **검증**: 백엔드 단위 110(103+7)·e2e 129(120+9 — 일반+공수+팀 반장/팀원 집계 정확·권한 격리·기간(분기)·PDF·잘못된 파라미터 400), pdftotext 표(총계·월별 12행·상대별·종소세 5줄) 확인, 집계 수동 대조(반장 총청구 870,000·팀지급분 420,000·순소득 450,000·삼성물산 600,000/2건). flutter analyze 0·test 77(74+3), iOS 시뮬 integration_test All passed+스크린샷 3장(`app/screenshots/income-01~03.png` — 총계 카드/팀 지급분·순소득/막대차트/상대별/종소세/PDF 버튼) 눈검수. 임시 pg 5436/백엔드 3040만 사용·정리, live-db(5432) 무손상. **P2 전 배치(P2a~P2d) 구현 완료**.

- ✅ **백로그 정리 — revoke 엔드포인트 · 웹 다중 사업장 전환 · 웹 계약서 목록** (`worklog/2026-07-11-백로그정리.md`, backend+web 만·app 무수정). **① revoke 공백 해소**: `POST /confirmations/:id/revoke`(발행자)·`POST /biz/contracts/:id/revoke`(사업장) — **SENT 만 무효화(→public 403)**, **SIGNED 는 증빙 보존 위해 링크 열람 유지·409**, DRAFT 409, 권한 격리 404. UI 노출은 백로그 유지(API 만). **② 웹 다중 사업장 전환**: `biz/layout` 사업장 선택 드롭다운(소유 2+일 때만·localStorage 저장·컨텍스트 전파), inbox/settlements/safety/contracts 가 선택 businessId 스코프로 동작(백엔드 `?businessId=` **additive**, 미소유 유출 차단). 단일 사업장 미노출. **③ 웹 계약서 목록(읽기전용)** `/biz/contracts`(상태 배지·PDF·외부 서명 링크 복사, 작성·서명은 앱 안내). **검증**: 백엔드 단위 110·e2e **137**(신규 revoke 8), web build 0, playwright 2사업장 시드 전환→inbox 스코프 라이브 변경(250,000↔330,000)·계약서 목록·단일 미노출 스크린샷 4장(`.playwright-mcp/backlog-0{1..4}-*.png`) 눈검수. 임시 pg 5437/백엔드 3050/web 3004 정리, live-db(5432) 무손상.

- ✅ **P2 통합 검수 완료 — 판정: P2 완료 가능(치명·중요 결함 0건)** (`worklog/2026-07-11-P2-통합검수.md`). 독립 QA(구현 4배치와 분리)가 배치 경계·회귀·정책 일관성 검수. **회귀 5종 전부 재현**(백엔드 단위 110·e2e 129(maxWorkers:1)·web build 0·flutter analyze 0·flutter test 77). **배치 경계 라이브 실측**(임시 pg 5436/백엔드 3040/web 3004, curl+웹 SSR+pdftotext): ①팀(P2a)→서명(P2b)→파생→소득 리포트(P2d) 수동 계산 정합 일치(반장 청구 620,000·팀지급분 420,000(수기 미가입 포함)·순소득 200,000·팀원 파생 270,000, 소득 PDF 동일), 웹 `/c` 팀 명단 표 렌더(P2a→P2b 이월) ②계약서 서명+같은 작업자 TBM ack→안전 리포트 PDF TBM 섹션(참석/확인 2/1)+safety_logs 정합, 재ack 409 ③웹 `/lc` 계약서 ko+vi(조항 번역·값 원문). **정책 3건 권고**: teamPayout 수기 포함 적절(현행 유지)·세금계산서 발행분 미구분 문제 아님(소득세는 발행 무관)·P2b 백로그(revoke·웹 계약서 UI) 출시 차단 아님(확인서와 일관한 공백). **코드 리뷰**: 권한 격리(business.ownerId 강제·e2e 커버)·public 토큰 만료 정책(계약서=확인서, 만료없음·revoke만, 일관)·TBM 당일 창 KST 정확·income-report 인메모리 로드 현 규모 허용. **live-db(5432) 무손상**, 소스 트리 무변경. 미해결은 백로그·수동 QA 이관.

- ✅ **P3a 수금 독촉 자동화 + 지급 평판 배지** (`worklog/2026-07-11-P3a-독촉평판.md`) — backend+app+web 완료. 마이그레이션 `20260711100000_p3a_reminders_badge`(additive: LedgerEntry `autoRemind`/`reminders[]`, Business 배지 캐시 3컬럼, Profile payout 계좌 3컬럼). **① 독촉**: `PATCH /ledger/:id` autoRemind 토글(파생 제외), `POST /ledger/:id/remind`(수동, 쿨다운 3일 409 REMIND_COOLDOWN), 크론 10:00 KST(D+7/D+30·연결 사업장=푸시+알림/수기=알림톡 `PAYMENT_REMINDER`+확인서 공개링크+금액+계좌(프로필 payout 신설)·같은 단계 중복 방지·문구 점잖게). **② 배지**: 평균 지급 소요일(SIGNED→전액PAID, 최근 12개월·표본 3건↑) 우수(≤15)/양호(≤30)만 노출(부정 낙인 금지), Business 캐시 컬럼+일일 크론(04:00)+pay 비동기 갱신, `GET /businesses/search`·`/businesses/:id`(신설) additive `paymentBadge`, `GET /biz/payment-badge`(본인+개선 안내). **검증(실측)**: 백엔드 단위 **137**(110+27)·e2e **146**(137+9, `test/p3a.e2e-spec.ts` — 토글→크론시뮬→이력·중복방지→쿨다운 409→표본 3건 경계→검색/단건 배지→권한 격리), build 0, 라이브 aggregation 실측. 웹 `BizPaymentBadge` 사업장 홈 자기 배지 — build 0·playwright 실측 EXCELLENT/GOOD 2상태. 앱: 장부 항목 자동 안내 토글+"지금 안내 보내기"+발송 이력, 프로필 입금 계좌(선택), 사업장 홈 자기 배지+`PaymentBadgeChip`(확인서 폼 연결 사업장), **신규 25키×6 ARB(666키 패리티 0결손)**, flutter analyze 0·test 77·iOS 시뮬 integration_test All passed+**스크린샷 4장**(`app/screenshots/p3a-01~04*.png`, EXCELLENT 상태 포함) Read 눈검수. **미완/이관**: 앱 사업장 검색 결과 리스트 UI 자체가 미존재 → 검색 배지는 모델·칩 준비 완료(향후 검색 UI 신설 시 부착), 알림톡 PAYMENT_REMINDER 카카오 템플릿 승인 필요(미승인 시 로그만). 임시 pg 5436/백엔드 3040/web 3004 만 사용 후 정리, **live-db(5432) 무손상**.

- ✅ **P3b QR 명함 (작업자 공개 프로필)** (`worklog/2026-07-11-P3b-QR명함.md`) — backend+web+app 완료. 마이그레이션 `20260711110000_p3b_qr_card`(additive: Profile `cardToken @unique`/`cardEnabled`/`cardIntro`/`cardViewCount`). **백엔드**: 신규 `card` 모듈 — `GET /me/card`(토큰 nanoid32 **최초 조회 시 lazy 생성**·URL `/p/{token}`·공개 미리보기·**본인 전용 expiredDocs**), `POST /me/card/rotate`(재발급→구 토큰 404), `PATCH /me` cardEnabled/cardIntro additive, `GET /public/profiles/:token`(@Public — 이름·업종·소개·**서류 유효 배지**(만료 등록≥1 && 만료 지남 0, 유형명·개수만)·장비 **종류만**·가입일·연결 안내(딥링크/스토어 자리), **전화·계좌·서류 파일 절대 비노출**, viewCount 만 증가, OFF/무효 404). 서류 판정 순수 함수 `card.util.ts`. **웹**: 신규 `/p/[token]` 공개 프로필(모바일 우선·시안 토큰·서류 유효 ✓ 배지·"이 작업자와 연결하기" CTA·OG 태그), i18n 신규 10키×6언어(패리티 컴파일 강제). **앱**: 더보기→"내 QR 명함"(qr_flutter 로컬 QR·URL 공유 share_plus·소개 편집·노출 ON/OFF·토큰 재발급·서류 유효 미리보기+만료 서류 본인 표시), 신규 18키×6 ARB(**684키 패리티 0결손**). **검증(실측)**: 백엔드 단위 **145**(137+8)·신규 p3b e2e **11**(발급→공개 조회→**비노출 필드 검증**→rotate 구토큰 404→OFF 404→viewCount) 전부 통과, e2e 회귀 150/157(documents 7건 실패는 **P3b 무관 — 기존 테스트의 '2026-07-11' D-0 하드코딩 시한부**, 당일 날짜 치환 시 10/10 통과 확인 후 원복·백로그 이관), build·lint 0. 웹 build 0·playwright 실측 3장(`p3b-web-01-ko/02-vi/03-404.png`, 루트) Read 눈검수. 앱 analyze 0·test **81**(77+4)·iOS 시뮬 drive All passed+스크린샷 3장(`app/screenshots/p3b-qr-01~03*.png`, QR 실렌더) 구현·오케스트레이터 양쪽 눈검수. 임시 pg 5436/백엔드 3040/web 3004 만 사용 후 정리, **live-db(5432) 무손상**. **P3-신규 3항목(독촉·평판·QR명함) 전부 구현 완료.**
- ✅ **P3 통합 검수 완료 — 판정: P3 완료 가능(치명·중요 결함 0건)** (`worklog/2026-07-12-P3-통합검수.md`). 독립 QA(구현 2배치와 분리). **선행 수정**: `test/documents.e2e-spec.ts:83` 날짜 하드코딩(`'2026-07-11'` D-0)을 동적(`kstTodayIso`, dday.util 과 동일 KST 기준)으로 교체 → documents.e2e **10/10 통과**(유일 허용 코드 변경, 그 외 소스 무변경). **회귀 5종 전부 재현**: 백엔드 단위 **145**·e2e **157**(documents 10 포함, 두 worklog 수치 정확 일치)·web build 0(`/p/[token]` 생성)·flutter analyze 0·flutter test **81**. **배치 경계 라이브 실측**(임시 pg 5436/백엔드 3040/web 3004, curl): ①독촉 전과정 — autoRemind 토글→수동 remind alimtalk(문구·금액·계좌·`/c/{shareToken}`+포트3004 링크 로그 실측)→쿨다운 **409 REMIND_COOLDOWN**→이력 append ②평판 — `/biz/payment-badge` INSUFFICIENT(부정 낙인 없음)·search/`:id` `paymentBadge` 키 존재(없으면 null)·**P3b 공개 명함에 사업장 배지 미노출 확인(키 부재, 의도)** ③QR — rotate 구토큰 404↔신 토큰 200·OFF 404·무효 404·**공개 응답 민감정보 재감사(실응답 grep: 계좌·전화·장비 spec·차량번호·파일경로 전부 CLEAN)** ④독촉 링크 ?lang= 미부착이나 `resolveServerLang` 폴백(?lang=→쿠키→Accept-Language→ko)으로 정합. **코드 리뷰**: 크론 3개(04/09/10시) 슬롯 분리·9시 문서만료와 공유하나 테이블 disjoint·멱등, cardToken nanoid32(≈192bit, shareToken 32 일관), viewCount 원자적 increment, 배지 pay 후 커밋 재집계 fire-and-forget+일일크론 정합, 마이그레이션 additive-only. **관찰(LOW·백로그)**: autoReminderStage 정확일치(`dday===-7`)로 크론 결번·늦은 토글 시 단계 영구 미발송(`<=` 완화 권고), reminders Json[] 상한 부재(수동 장기 반복 시 증가—cap N 권고), 알림톡 템플릿 승인·스토어 딥링크 env·앱 검색 UI(기존 이관). **live-db(5432) 무손상**(Up 5 days, .env 원본 유지), 임시 자원 전부 정리.

- ✅ **리프레시 토큰 기반 자동 로그인 연장** (`worklog/2026-07-17-리프레시토큰.md`, backend+app 만·**web 무수정**). 마이그레이션 1개 `20260717120000_refresh_tokens`(additive: `refresh_tokens` — profileId FK cascade·tokenHash sha256 @unique·expiresAt 슬라이딩 180일·lastUsedAt·deviceId?·revokedAt, 인덱스 3). **액세스 토큰 30분 단축**(env `ACCESS_TOKEN_TTL` 기본 `30m`, JwtModule 전역 7d 유지→**기존 7일 토큰 만료까지 유효·가드 무변경·하위호환**). **발급**: verify/kakao 성공 시 액세스+리프레시(불투명 hex 64자, 해시만 저장) 동시 발급, 응답 `refreshToken` **additive**+선택 `deviceId`. **`POST /auth/refresh`(@Public)**: 회전(rotation)—기존 폐기+새 발급·만료 180일 재연장, **재사용 감지**(폐기 토큰 재사용→프로필 전체 폐기+401 `REFRESH_REUSED`), invalid/expired 401. **`POST /auth/logout`**: 해당 리프레시만 폐기. **활성 상한 5(기기 5대)**·**정리 크론 주1회(일 03:00 KST)**. **앱**: token_store 리프레시 secure storage 별도 키, api_client **401→자동 refresh→원요청 1회 재시도, 동시 다중 401 single-flight 뮤텍스 합류**, 실패 시에만 강제 로그아웃(기존 플로우), logout 버튼 서버 logout 후 로컬 클리어. **신규 사용자 문구 불필요→ARB 무변경**(자동 refresh 무음). **검증(실측)**: 백엔드 단위 **164**(155+신규 refresh-token 9: 회전·재사용감지·상한·슬라이딩·revoke·cleanup)·e2e **162**(157+신규 5: 만료→refresh→/me 200·재사용 전체폐기 401·logout 후 401·invalid 401), tsc/build 0, **실서버 curl(pg 5438/api 3060, ACCESS_TTL=3s) 로그인→만료→refresh 회전→재사용 REFRESH_REUSED→logout 전 과정 실측**. 앱 analyze 0·test **85**(81+신규 4: 401→refresh→재시도·**동시 4요청 single-flight 1회 합류**·실패 강제로그아웃·토큰없음). **iOS 시뮬(iPhone 17) integration_test 통과** — 로그인→홈→23초 대기(액세스 20s 만료)→탭 이동 끊김 없음(재로그인 미노출), **DB 실측 이동 중 refresh 1회 회전(single-flight 합류 증거)**. **포트: 임시 pg 5438·백엔드 3060 만 사용, live-db(5432)·사용자 테스트 스택(5439/3010/3001)·타 에이전트(5436/3040) 무접촉**, backend/.env 원본 무변경. **미완/백로그**: 앱 deviceId 미주입(백엔드 선택 수용·상한은 동작).
- ✅ **P4 문자 연동** (`worklog/2026-07-17-P4-문자연동.md`, **app/ 만·backend·web 무수정**). 설계 확정본대로 **기기 문자앱 호출+프리필**(자동발송·기본문자앱 등록 없음, 정책 무리스크). **① SmsComposer 브릿지**(MethodChannel `kr.workon/sms`): `core/sms_composer.dart`(canSend/compose+폴백 share_plus/`sms:`), iOS `SmsComposerPlugin.swift`(MFMessageComposeViewController recipients/body/이미지·PDF 첨부, 시뮬 canSendText=false→Dart 폴백; AppDelegate 등록·pbxproj 추가), Android `SmsComposerPlugin.kt`+MainActivity(텍스트 `ACTION_SENDTO smsto:`/이미지 `ACTION_SEND`+FileProvider+기본문자앱, 실패 시 chooser 폴백; manifest FileProvider+`<queries>`). 이미지 전처리 `core/image_compress.dart`(순수 Dart `image` 패키지, EXIF 보정·≤1024px·≤300KB 반복압축, bizconnect `compressImageWithExif` 재구현). **② 문자로 보내기 버튼**: 확인서 상세(수기 상대 전화 자동 수신인+서명부탁 본문, +전화 걸기)·QR 명함·서류 묶음 공유. **③ 빠른 보내기**(더보기): 기본 3종(명함/사업자등록증/통장사본—서류 지갑 유형매칭·마스킹본 우선·`이미지로 첨부` 옵션)+커스텀 템플릿(shared_preferences)+변수치환 `core/sms_template.dart`(bizconnect TemplateEngine 재구현 `{내이름}{상대명}{날짜}` 등). **④ 통화 후 카드** `core/call_log.dart`(복귀 시 10분내 미제안 통화 제안, 같은통화 1회·설정 기본ON·권한불필요)+`widgets/post_call_card.dart`(main_shell 상단, 명함/빠른보내기/닫기)+더보기 토글. 신규 47키×6 ARB. 의존성 `url_launcher`·`image`. **검증**: analyze 0·test **108**(회귀 91+신규 17: 템플릿7·이미지압축4·통화카드6), `build apk --debug`·`build ios --simulator` 통과, **별도 시뮬(`workon-p4-verify` iPhone16 iOS26.5 신규생성, 사용자 booted iPhone 17 무점유) drive All passed**+스크린샷 5장(`screenshots/p4_01~05*.png`, 통화 후 카드·수신인 프리필 01099998888·설정토글) 눈검수, FakeComposer 로 수신인·명함링크·내이름 프리필 실측. 임시 시뮬 삭제, 백엔드/DB 미기동(인앱 Fake), live-db(5432)·사용자 스택(5439/3010/3001) 완전 무접촉. **한계**: 문자 작성창 실렌더는 실기기 필요(시뮬 canSendText=false→폴백), 연결목록/작업상세는 모델에 전화번호 없어 직접 전화버튼 미부착(백로그), 원어민 번역 검수 권장.
- ✅ **생체/PIN 앱 잠금 + 카카오 로그인 네이티브 준비** (`worklog/2026-07-17-생체잠금-카카오준비.md`, **app/ 만·backend·web 무수정**). **① 앱 잠금(local_auth 2.3.0)**: `core/app_lock.dart` 상태 머신(StateNotifier+WidgetsBindingObserver) — 앱 시작 잠금·**백그라운드 30초↑ 복귀 재잠금**·인증 성공/명시적 로그인 해제, `isDeviceSupported()`+`biometricOnly:false`(생체 실패/미지원 시 **기기 암호 폴백**), 리프레시 자동연장과 독립된 **로컬 게이트**. `features/auth/lock_screen.dart` — `AppLockGate`(MaterialApp.router builder 오버레이, `잠금&&인증됨`일 때만·**`unknown→인증` 세션복원은 잠금 유지·`unauthenticated→인증` 명시 로그인만 해제**), `LockScreen`(브랜드 아이콘+자동 인증+재시도 유지+로그아웃). 더보기>설정 "앱 잠금" 스위치(기본 OFF, 미지원 안내). main 에서 SharedPreferences 초기값 override. **신규 6키×6 ARB**. iOS `NSFaceIDUsageDescription`(한국어), Android `USE_BIOMETRIC`+`FlutterFragmentActivity`. **② 카카오 네이티브 준비(키 없이 완결)**: iOS `Info.plist` `CFBundleURLSchemes kakao$(KAKAO_APP_KEY)`+`LSApplicationQueriesSchemes`, 빌드 설정 `KAKAO_APP_KEY`(xcconfig 빈 값 기본→무해). Android `manifestPlaceholders[KAKAO_APP_KEY]`+`AuthCodeHandlerActivity` redirect(빈 값 no-op)+kakaotalk queries. **dart-define 하나로 버튼 노출→SDK init→로그인 호출 전 체인** 확인. DEPLOY 6절 "카카오 키 최종 주입 위치"(앱 dart-define + 백엔드 env **2군데**) 갱신. **검증**: analyze 0·test **91**(85+신규 app_lock 6: 시작잠금·30초타이머·OFF통과·인증성공실패·setEnabled·markUnlocked). **별도 시뮬(`workon-lock-verify` iOS26.5, 사용자 booted 기기 무점유) drive `lock_sim_test` All passed + 스크린샷 3장**(OFF 바로진입·ON 잠금화면·인증→진입, `app/screenshots/lock_0*.png`) Read 눈검수(초기 세션복원 즉시해제 버그 1건 시뮬서 발견→수정). **iOS(--simulator)·APK 빌드 KAKAO_APP_KEY 없이 통과**. **한계**: 실제 생체 시뮬 조작 미수행(시뮬 미설정·CLI 불안정)→인증기 override 로 게이트 결정적 검증, 실기기 생체 QA 권장. 검증 임시 시뮬 삭제, live-db·사용자 스택 무접촉.
- ✅ **[치명] PDF 글리프 깨짐 수정 + 문서 6종 리디자인** (`worklog/2026-07-18-PDF개선.md`, **backend 만·app·web 무수정**). 감사 ★1(릴리스 블로커) 해소. **① 원인 규명(재현 확정)**: `pdf.service.ts` 가 `embedFont(bytes,{subset:true})` 사용 → `@pdf-lib/fontkit@1.1.1` 서브셋 매핑 버그로 **숫자 0~9 전부·한글 다수 글리프 미렌더**(텍스트 레이어·`emb=yes` 는 정상이라 기존 텍스트 검사로 미검출). 동일 폰트 subset true/false 최소 재현으로 확정. **② 수정**: **`subset:false`(전체 임베드)** 전환 → 글리프 정상. 타이포 위계용 **NanumGothic Bold 로컬 OFL 재확보**(`mybot` 캐시→`assets/fonts/NanumGothic-Bold.ttf`, 외부 다운로드 없음) 병행 임베드. `pdffonts` 재확인 `emb=yes/sub=no`. **③ 리디자인(DESIGN-IMPROVE P0-2)**: 공통 헬퍼로 6종 통일 — 문서명 헤더(24pt 굵게)+발행처(작업온·workon)+발행일, 명확한 괘선 표·금액 우측정렬(tabular)·미수/입금 색 병행, **합계 강조 박스**(옅은 오렌지), **서명 도장 박스**(이중 테두리+서명 이미지+서명자명+일시, 계약서는 양측 2개), 하단 "작업온에서 발행 · workon" 푸터. DESIGN-UI 팔레트·여백 통일, 과한 장식 배제. 리디자인 중 명세서 컬럼 겹침·현장인건비 헤더 괘선 겹침 버그 수정. **④ 검증(필수 기준 충족)**: 6종(서명 확인서·팀 확인서·양측 서명 계약서 포함) 생성→`pdftoppm -png -r 100`→**Read 눈검수 전부 정상**(숫자·한글·서명 이미지·표 정렬). 전후 이미지 `web/screenshots-audit/pdf/`(before 깨짐) ↔ `web/screenshots-audit/pdf-fixed/`(after 정상). **글리프 검증 자동화 1건 신규**(`pdf.service.glyph.spec.ts` — 전체 임베드 바이트>300KB 가드 + **pdftoppm 픽셀 밀도 검사** 정상0.008/깨짐0.0027 경계0.005, poppler 없으면 skip). **단위 186**(184+2)·**e2e 172** 통과, lint/build 0. 회귀는 **임시 pg 5438·PORT 3060** 만 사용 후 컨테이너 삭제, **live-db(5432)·사용자/타에이전트 스택(5437/5439/5440·3010/3001) 무접촉**·.env 무변경.

## 확정된 결정
- 업종 포괄 (장비 기사 + 일반 노동자), 장비 사용자는 서류 관리 강화
- 스택: Flutter + Next.js + NestJS + PostgreSQL, **자체 리눅스 서버 Docker 운영 (Supabase 사용 안 함)**
- 플랫폼: 안드로이드 + iOS + 웹 / 로그인: 카카오 + 전화번호 인증
- 진행 순서: 화면 디자인 시안 먼저 → 확정 후 구현

## 다음 할 일 (개발 완료 — 배포 및 백로그)
- ~~**[디자인 후속 배치] 홈·캘린더 정보 위계**~~ ✅ 완료(f42f170) + ~~아이콘 통일·PDF 전 종 렌더·biz 웹/명함 시각 검증~~ ✅ 완료(`worklog/2026-07-18-백로그마감.md` — 아이콘 18종 44곳 아웃라인 수렴, PDF 7종 렌더 전부 합격·수정 0건, biz 웹 4화면+QR 명함 합격). **원 감사(5.2/10) Top 지적 중 코드로 남은 미해결 항목 없음.** 잔여 권고(낮음): 모바일 표 우측 컬럼 스크롤 위치, /p 기본 언어 정책 확인, 캘린더 미수/입금 분리 표기(확인서 모델 확장 필요).
- **구인구직 매칭 보류** (2026-07-18 결정, STRATEGY.md): 연결망 형성 후 "검증된 매칭"으로 재검토. 중간 단계 "빈 날 공유" 백로그.
- **실제 서버 배포**: `docs/DEPLOY.md` 체크리스트 그대로 진행(사용자 직접). 외부 키 4종 발급이 사용자 몫:
  카카오 REST 키(developers.kakao.com), 기상청 KMA_SERVICE_KEY(data.go.kr 활용신청), 공공데이터 GOV_DATA_SERVICE_KEY(사업자진위), FCM 서비스계정 JSON + 앱 설정파일(GoogleService-Info.plist·google-services.json·firebase_options.dart). 미발급 시 해당 기능만 비활성 스텁.
- ✅ **앱스토어 출시 준비 완료** (`worklog/2026-07-11-출시준비.md`) — **아이콘·스플래시·릴리스 설정·스토어 자료**. 앱 아이콘(오렌지 #F4770C + 흰 "온", 웹 파비콘 통일, iOS 다크/틴트 변형 포함) + 스플래시(종이톤 배경 + 오렌지 "온")를 **로컬 PIL 렌더**(`app/tool/gen_brand_assets.py`, 외부 다운로드 없음) → flutter_launcher_icons/flutter_native_splash 로 전 사이즈 생성. iOS `CFBundleDisplayName=작업온`, Android `label=작업온`. **ATS 조건화**: 소스 Info.plist 에서 `NSAppTransportSecurity` 제거(Release=HTTPS 강제) + Debug 전용 "Inject Debug ATS" 빌드 페이즈(PlistBuddy)로 로컬 HTTP만 주입. Android **릴리스 서명 골격**(key.properties 방식·debug 폴백·`.gitignore` 확인) + **minify/shrink + proguard** + **main manifest 권한 정리(INTERNET 등 — release 무네트워크 버그 수정)**. **검증**: analyze 0·test 77(회귀 없음)·`build ios --simulator`(Debug ATS 주입 확인)·`build ios --release --no-codesign`(ATS 제거 확인·1.0.0+1)·`build apk --debug`·`build apk --release`(minify) 전부 통과, 시뮬 눈검수 3장(`app/screenshots/release-01~03`: 홈 아이콘·스플래시·앱 기동). 스토어 자료 `docs/STORE.md`(이름/설명/키워드/카테고리/스크린샷 6장 가이드/데모계정/개인정보처리방침 초안). backend/web 무변경·live-db 무손상. **사용자 몫**: 릴리스 키스토어 생성, 개발자 계정, 개인정보처리방침 URL 호스팅, 심사용 데모계정(백엔드), `--dart-define=BASE_URL=`.
  - 기존 참고(DEPLOY.md 6절): `--dart-define=BASE_URL=https://도메인/api`, ~~iOS 배포타깃 15.0 통일~~(✅ 완료), 카카오 로그인 버튼 노출 조건(전화인증만으로 온보딩 완결).
- **미구현 백로그(코드)**: ~~사업장 다중 소유 전환 웹 UI~~(✅ `worklog/2026-07-11-백로그정리.md` — 드롭다운·businessId 스코프·웹 계약서 목록·revoke API 완료), revoke 웹/앱 UI 노출(현재 API 만), app(Flutter) 다중 사업장 전환 UI, 승격 전역스캔 DB측 필터 최적화, safety_logs DB 트리거(증거력 강화 시), ~~**documents.e2e 날짜 하드코딩 수정**~~(✅ P3 통합검수에서 `test/documents.e2e-spec.ts:83` 을 `kstTodayIso` 동적 날짜로 교체 → documents.e2e 10/10 통과), ~~**[P3 신규] autoReminderStage 정확일치 완화**~~(✅ 2026-07-12 `worklog/2026-07-12-P3-통합검수.md` §4 — `autoReminderStage(dday, reminders)` 를 `dday<=-30`/`dday<=-7` "이상 경과+미발송" 판정으로 완화, 동시 조건 시 D30 하나만·D30 후 D7 역행 방지·dedup 정합 유지), ~~**[P3 신규] reminders Json[] 상한(cap N)**~~(✅ 2026-07-12 `appendReminder`+`REMINDER_HISTORY_CAP=20`, viewLogs cap 패턴, 최근 20건만 유지). 재검증 단위 155·e2e 157(pg 5436)·lint/build 0·live-db 무손상.
- ✅ **P1 전 항목 반영 완료**(공수·오프라인·세금계산서·카카오 UI·다국어·**홈 위젯**). **P1 통합 검수만 남음** — 특히 홈 위젯은 GUI 환경에서 iOS/Android 홈 배치·소형/중형·다크·탭 눈검수 필요(`worklog/2026-07-11-P1-위젯.md` "미검증" 참조).
- **카카오 실연동 네이티브 설정**: 앱은 UI 조건부 노출까지 완료. 실제 활성화 시 iOS Info.plist(URL scheme·QueriesSchemes)·Android manifest(redirect activity·KAKAO_APP_KEY) + `KAKAO_ENABLED=true` 필요(DEPLOY.md 6절).
- ✅ **앱 다국어 완료** (`worklog/2026-07-11-P1-다국어-앱.md`). 남은 다국어 후속: 확인서 PDF 이중언어 옵션(백엔드)·알림/알림톡 본문 다국어(백엔드)·ApiException 코드→문구 매핑·서류유형 표시 번역·원어민 검수(gongsu/조출 등).
- **P1 외부 키 대기**: 알림톡(Solapi + 카카오 비즈메시지 템플릿 승인), 카카오 로그인(`KAKAO_ENABLED=true`) — 미발급 시 해당 기능만 비활성(로그/501), 나머지 정상.
