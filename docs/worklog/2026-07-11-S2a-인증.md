# S2a 스키마 보강 + 인증 + 공통 인프라 (2026-07-11)

## 구현
- 스키마: Profile.kakaoId, name nullable / DocumentShare.useMasked / Job.acceptedAt / SafetyLog.businessId FK / Confirmation @@index(profileId,date) / OtpCode 테이블 — 마이그레이션 `20260711014741_s2a_auth_otp`
- 공통: `{data}`/`{error:{code,message}}` 봉투, 전역 JwtAuthGuard+@Public+@CurrentUser({userId}), prod 약한 JWT_SECRET 기동 차단
- 인증: phone request(3분 만료·30초 쿨다운·bcrypt 해시·dev에서만 devCode) / verify(5회 제한, JWT 7일) / kakao(설정 없으면 501 스텁, 실호출 코드 있음) / GET·PATCH /me (전화검색 동의 포함, hasBusiness는 파생)
- 의존성: @nestjs/jwt, bcryptjs, jest/supertest

## 검증 (실제 실행)
- 단위 9/9, e2e 3/3, lint 에러 0, build 통과
- 실서버 curl: 쿨다운 429 / verify→JWT / 무토큰 401 / PATCH 반영 / 5회 차단 429 / kakao 501 / health 봉투 제외 — 전부 확인
- 임시 pg(5433) 사용 후 정리, live-db 무손상

## S2b 이월 주의
- **서류별 원본/마스킹 선택**: `_SharedDocuments` 암묵 조인을 명시적 조인 모델로 승격해 per-doc 플래그 필요 (useMasked는 묶음 기본값)
- 카카오 가입자 phone은 `kakao:{id}` placeholder → 온보딩 전화인증으로 승격 규칙 추후 확정
- 로컬 dev DB는 5433 (5432는 타 프로젝트 live-db)
- git 미초기화 상태 — 커밋은 사용자 요청 시
