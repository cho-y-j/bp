# S1 기반 골격 구현 (2026-07-11)

## 구현 (Opus 에이전트)
- backend: NestJS 11 + Prisma 6.19 + PostgreSQL 16, 전역 prefix /api(health 제외), GET /health(DB 체크)
- 스키마 13테이블+조인1: profiles, businesses, connections, equipments, documents(다형 소유자+maskedFilePath+expiryDate 인덱스), document_shares(선택 묶음+viewLogs), jobs/confirmations/ledger_entries(미가입 상대 수기 병행), work_logs, safety_logs(**append-only, updatedAt 없음**), notifications, device_tokens
- web: Next.js 15 App Router, /health 페이지
- infra: docker-compose(운영: pg+api+web+nginx), docker-compose.dev(pg만), nginx.conf
- 버전 핀: 갓 나온 파괴적 버전(Prisma 7, Next 16, TS 7) 회피 — NestJS 11/Prisma 6.19.3/TS 5.9.3/Next 15.5.20

## 구현자 검증 (실행 근거 있음)
- prisma validate/migrate 성공(임시 5433 pg로, 로컬 5432는 live-db 점유), backend·web 빌드/린트 통과, start:dev 후 /health 200 {"database":"up"}
- 미검증: 통합 compose(api/web/nginx) 빌드·기동

## 독립 검수 결과: ✅ S2 진행 가능 (치명 0)
- 통합 compose 실기동 검증 통과: 빌드→기동→마이그레이션→nginx 3경로(/health 200, / 웹, /api 404) 모두 정상. 테스트 자원 전부 정리, 기존 컨테이너 무손상
- compose 함정 발견: override에서 ports는 병합됨 → `ports: !override` 필요 (배포 문서에 반영할 것)
- S2 편입 지적: [중요] Profile에 kakaoId 필드 / document_shares.useMasked(마스킹 정책 모순 해소) / api·web healthcheck+nginx service_healthy
- [보안] 루트 .gitignore 부재 → **즉시 조치 완료(.env/ref 등 제외)**, 약한 기본 시크릿(change-me)은 prod fail-fast 필요
- [사소] jobs 수락 상태, safety_logs FK, confirmations date 인덱스, api 컨테이너 root 실행, / 경로 웹-API 충돌, OTP 저장 방식 — 각 도메인 구현 시 반영

## 메모 (S2 이월)
- 미가입 상대 → 가입 시 전화번호 매칭 승격 규칙 API에서 확정 필요
- safety_logs append-only는 앱 레벨 규약 — 증거력 강화 시 DB 트리거 검토
- 로컬 5432는 live-db 점유 → dev는 5433 사용 습관화
