# 디자인 백로그 마감 — 아이콘 통일 · PDF 전 종 렌더 · biz 웹/명함 시각 검증 (2026-07-18)

> 재감사(`2026-07-18-디자인재감사.md`) 잔여 항목 3건. 병렬 Opus 에이전트 2개(아이콘 / 시드+검증), 오케스트레이터 교차 눈검수.

## ① 앱 아이콘 아웃라인 통일 (★10 해소, app/ 만)
- filled·rounded 계열 **18종 44곳 → `_outlined`/스트로크형 수렴** (28파일, 신규 의존성 없음). 대표: verified_rounded→verified_outlined(9곳), check_circle_rounded→check_circle_outline_rounded(5), lock/security/bolt/draw/copy/agriculture/storefront/sms/picture_as_pdf 등.
- **의도적 예외 3그룹**(보고·수용): ①하단 탭 활성 상태 filled(선택 구분 UX) ②배지 안 check_rounded·warning_amber_rounded(시각 균형, 본래 스트로크형) ③chevron/arrow/search 등 순수 스트로크형(이미 톤 일치).
- 검증: analyze 클린·flutter test 115 통과·스크린샷 2장(`app/screenshots/uxfix-10-icons-home.png`·`uxfix-11-icons-wallet.png`) 구현·오케스트레이터 양쪽 눈검수 — 본문 아웃라인 일관, 활성 탭만 filled.

## ② 시드 보강 (3070/5440, API 호출로만·기존 데이터 미삭제)
사업주 박현장(010-2222-3333)·사업장 온플러스건설·작업자 연결 ACCEPTED·작업지시 3건(예정/진행/완료)·biz 서명 확인서 3건(서명 이미지 포함)·팀 확인서 SIGNED(2인)·근로계약서 양측 SIGNED·정산 지급 3건(690,000)·TBM 1건·장비 2건·QR 명함 토큰 활성.

## ③ PDF 전 종 렌더 회귀 (pdftoppm -r 100 → Read 눈검수)
**7종 전부 합격, backend 수정 0건**: 작업확인서(서명 도장 이미지 실렌더)·팀 확인서(명단표 470,000)·근로계약서(**양측 손글씨 획 2개**)·거래명세서(입금 녹/미수 적 색분리)·연간 소득 리포트(2페이지·월별 12개월)·현장별 인건비(3현장+총계 1,410,000)·안전관리 리포트(TBM 표·발송/확인 기록). 재감사 미확인 5종 공백 해소.
- 참고: 레거시 확인서(서명 이미지 없이 SIGNED 시드된 구 데이터)는 "(미서명)" 렌더 — 데이터 한정 현상, 신규 서명건 3건으로 로직 정상 확인.

## ④ biz 웹·QR 명함 시각 검증 (격리 웹 3004→API 3070, playwright 1280+390)
**5화면 전부 합격**: `/p` 명함(업종·장비 칩·연결 CTA·언어 토글, 서류 유효 배지는 valid=true 조건 가역 검증 후 원복)·`/biz/attendance`(통계 3/2/1/1 시드 일치·상태칩)·`/biz/site-costs`(접이표·총계·PDF)·`/biz/wage-statement`(3.3%↔일용 토글 667,230/682,880·마감·주민번호 경고). 스크린샷 `web/screenshots-audit/reaudit/`.
- 관찰(권고 수준): 모바일 390에서 표 우측 컬럼이 스크롤 뒤(overflow-x:auto, 하드클립 아님). `/p` 기본 언어가 환경에 따라 비한국어로 뜨는 건 resolveServerLang(Accept-Language) 동작 — 의도 여부 판단 보류.

## 종합
재감사 잔여 Top 5 중 ★10 아이콘(해결)·검증 공백(해소) 완료. 이로써 원 감사 Top 지적 중 코드로 남은 미해결 항목 없음. 사용자 스택(3010/3001/5439)·live-db(5432)·인도용 시뮬(iPhone 17 workon) 무접촉(아이콘 캡처 중 일시 shutdown 발생 → 부팅 상태로 복원됨).
