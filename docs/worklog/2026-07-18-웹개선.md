# 웹 디자인 개선 — P2 (2026-07-18)

> 근거: `docs/DESIGN-IMPROVE.md` P2 · `docs/worklog/2026-07-18-디자인감사.md`(★2·★7·★8·★9) · `docs/DESIGN-UI.md`.
> **web/ 만 수정**(backend·app 무수정, 기존 공개 API 소비). 원칙: "더 크게·더 명확하게·더 적게".
> before: `web/screenshots-audit/` · after: `web/screenshots-webimprove/`.

## 항목별 개선

### 1. 종이 확인서 서명 — 도장(인영) 박스 렌더 [★2]
- **공개 API 제약 확인(중요)**: `GET /public/confirmations/:token`(`confirmations.service.ts publicView`)와 사업장 수신 상세(`biz.service`)는 **서명 이미지 데이터/경로를 반환하지 않는다**(`signerName`·`signedAt` 만). 내부 `signImagePath` 는 매퍼에서 의도적으로 비노출(파일 서빙 공개 라우트 없음 — 컨트롤러 `:token`·`:token/pdf`·`:token/sign` 만). PDF 는 서버가 임베드하지만 화면용 이미지 접근 경로가 없다. → **손글씨 서명 이미지 실렌더는 백엔드 수정 필요**(아래 백엔드 요청사항).
- **web-only 개선**: 기존 "✓ 박사장 님 서명 완료" 초록 텍스트 배지를 **도장(인영) 박스**로 교체 — 붉은 인주색 원형 인영(서명자명 각인, 살짝 기울임·이중 테두리)에 서명자명 큰 글씨 + "서명 완료" ✓ + 서명 일시. 종이 확인서의 시각적 클라이맥스로 승격. 6언어 라벨 대응(`paperSignConfirmed` 키 신설).
- 파일: `components/PaperConfirmation.tsx`(`sealText()`·sign-plate), `app/globals.css`(`.sign-plate`·`.seal`·`--seal` 토큰 라이트/다크), 6개 사전 `paperSignConfirmed`.

### 2. 다국어 금액 — ₩ + 한국식 콤마 고정 [★8]
- `lib/format.ts money()` 를 로케일 그룹핑(`grouped()`, ru 공백·일부 로케일 점)에서 **항상 `won()`(ko-KR 콤마)** 로 일원화. 한국어 `1,050,000원`, 그 외 `₩1,050,000`. `grouped()` 폐기.
- **검증(6언어)**: `₩1.050.000`(vi, 점 오독) → `₩1,050,000`. ru `₩1 050 000`(공백) → `₩1,050,000`. ko/en/ne/zh 전부 콤마 확인.

### 3-a. QR 명함(/p) — 종이 명함 메타포 리디자인 [★7]
- 제네릭 카드 → **종이 명함**: 상단 오렌지 그라데이션 헤더(브랜드·"작업자 명함"), 큰 이름, 업종 역할 부제, 소개, **"서류 유효" 인증 스트립 강조**(초록·방패+체크, 감사 지적 "서류 유효 배지 강조"), 서류 유형·장비(트럭 아이콘) 칩. 데스크톱·모바일 반응형. `app/p/[token]/page.tsx`·`.namecard*` CSS.

### 3-b. 랜딩(/) — 한 문장 + 큰 CTA 1개 단순화 [★7]
- 2×2 대형 카드 그리드·하단 대량 공백 제거 → **수직 중앙 정렬**, 헤드라인 한 문장 + 서브 1줄 + **큰 CTA 1개**(사업장 웹 로그인) + 절제된 기능 칩 스트립. `app/page.tsx`·`.landing*` CSS.

### 4. biz 화면 정리 + D-day·상태 앱 규칙 통일 [★9]
- **죽은 공간**: `.biz-main` max-width 1000→**880px**(행이 과도하게 늘어나 이름↔금액 시선이동이 길던 문제 완화). 표/행 hover 하이라이트(`.data-table tr:hover`·`button.row-item:hover`)로 스캔성 강화.
- **D-day 통일**(`lib/format.ts`): `ddayBadge`(D+N 부호) 폐기 → **`expiryBadge`**(만료 지남="만료됨" 빨강 / ≤30 "D-N" / 31~90 차분 / >90 배지 생략)·**`collectBadge`**(기한 지남="기한 지남" 빨강 / 그 외 "수금 D-N"). 앱 `docExpired`·`ddayText(collectDday/statusOverdue)` 규칙과 일치. 적용: `/w/home`·`/w/documents`(expiryBadge)·`/w/ledger`(collectBadge)·`/s`(만료됨 i18n·원거리 생략, `docExpired` 6언어 신설).
- **검증**: `/w/documents` — 만료 지남 "만료됨"(빨강)·건강진단서 "D-12"(임박)·먼 만료 배지 생략. `/w/ledger` — 기한 지난 미수 "기한 지남".

### 5. 영문 enum 노출 [★3-웹]
- 웹은 전 화면 이미 한국어 라벨 매핑(수신함 서명완료/서명대기·출역 예정/수락/작업중/완료·계약서·작업자 연결됨/수락대기). **웹 신규 노출 없음** 확인(앱의 "(SIGNED)" 는 app/ 범위·본 작업 외).

## 검증
- **`npm run build`: 타입에러 0**(23 라우트). 격리 사본(`scratchpad/webbuild`, 사용자 `web/.next` 무접촉)에서 최종 소스와 동일 빌드.
- **임시 스택**: pg `workon-webimprove-pg`(**5437**) + 백엔드(**3050**, WEB_ORIGIN=3006 CORS) + 웹 격리 사본(**3006**). API 시드(작업자 김현수+사업장 대한건설(주) 연결, 서명 확인서 2·미서명 1, 서류 유효 3종·만료/임박, QR 명함).
- **playwright 전후 비교 스크린샷 10장**(`web/screenshots-webimprove/`, 데스크톱 1280+모바일 390) Read 눈검수:
  - `after-c-signed-ko.png`(도장 박스·1,050,000원)·`after-c-signed-vi.png`(₩1,050,000 콤마)·`after-c-signed-ne-mobile.png`(콤마+도장)
  - `after-p-card-ko.png`/`after-p-card-mobile.png`(명함+서류유효 스트립)
  - `after-landing.png`/`after-landing-mobile.png`(단순화)
  - `after-biz-inbox.png`(880px 정리·한국어 배지)
  - `after-w-documents-dday.png`(만료됨/D-12/원거리 생략)·`after-w-ledger-dday.png`(기한 지남)
- 6언어 금액 콤마 curl 대조(ru 공백→콤마 포함).
- **자원 정리**: 웹 3006·백엔드 3050 종료(PID 지정), pg 5437 컨테이너 삭제.
- **무손상 실측**: 사용자 web(3001) 200·api(3010) 200·live-db(5432) Up·workon-local-pg(5439) Up·타 에이전트 pg(5440) 무접촉. 사용자 `web/.next` 무접촉(빌드·dev 모두 격리 사본).

## 백엔드 요청사항 (서명 이미지 실렌더용 — app/backend 범위)
- 공개 확인서 API(`GET /public/confirmations/:token`)에 **서명 이미지 접근 수단** 추가 필요. 택1:
  1. 응답에 `signImageUrl`(공개 서명 이미지 라우트, 예 `/api/public/confirmations/:token/signature.png`) 추가, 또는
  2. base64 인라인(`signImageDataUrl`) — PDF 임베드에 이미 로드하므로 재사용 가능.
- 접근 가능해지면 웹 도장 박스 자리에 실제 서명 획을 렌더(백엔드 무수정 상태로도 `.seal` 인영으로 우선 개선 완료).

## 서명 이미지 실렌더 — 완료 추기 (2026-07-18, backend+web 소폭 / app 무수정)
- **백엔드(additive)**: 서명 이미지를 base64 PNG data URI 로 공개 응답에 노출(SIGNED/서명완료 시에만, PDF 임베드용 `readFile` 로직 재사용).
  - `confirmations.service.ts` — `publicView`·`bizConfirmationDetail` 에 `signImageDataUrl`(SIGNED 만). 신규 `loadSignDataUrl(path)` 헬퍼(파일→data URI, 실패 null).
  - `labor-contracts.service.ts` — `publicView` 에 양측 `employerSignImageDataUrl`·`workerSignImageDataUrl`(각 서명 완료된 쪽만). 동일 헬퍼.
  - 기존 필드 무변경·순수 additive. 크기: PNG 그대로(서명 저장 시 이미 ≤1MB 검증).
- **웹**: 도장(인영) 박스 자리에 실제 손글씨 획 `<img>` 렌더, 서명자명·"서명 완료"·일시는 그대로 유지, 이미지 없으면 기존 `.seal` 인영으로 폴백.
  - `PaperConfirmation.tsx`(`signImageDataUrl` 필드+조건 렌더), `PaperLaborContract.tsx`(양측 `sign-stroke-inline` 획), `globals.css`(`.sign-stroke`—흰 종이 바탕으로 라이트/다크 모두 획 가시).
  - biz 수신함 상세는 `/biz/confirmations/:id`(=`bizConfirmationDetail`) 응답으로 동일 컴포넌트가 자동 반영.
- **검증**: 
  - e2e 추가 — confirmations: 미서명 public `signImageDataUrl` 부재 + SIGNED public 에 `data:image/png;base64,` 포함. labor: 사업장 서명본(작업자 미서명) employer 획 有·worker 획 부재 + SIGNED 양측 획 有.
  - **전체 e2e 174 통과**(임시 pg **5438**, 기존 172 + 신규 2), 백엔드 타입체크 0.
  - **web build 0**(격리 사본 `scratchpad/webbuild`, 사용자 `web/.next` 무접촉, 24라우트).
  - **playwright 실측**: 임시 스택(pg 5438·백엔드 **3060**·web **3006**)에 서명 확인서 시드(손글씨 곡선 PNG)→ `/c/{token}` 스크린샷 `web/screenshots-webimprove/after-c-signed-signature-ko.png` Read 눈검수 — 도장 박스 자리에 **파란 손글씨 획 실렌더**·서명자명("이정호 소장")·"서명 완료"·일시 유지 확인.
- **자원 정리**: web 3006·백엔드 3060 종료(PID), 임시 pg 5438 컨테이너 삭제, 임시 시드 스크립트 삭제. **사용자 web(3001)·api(3010)·live-db(5432)·workon-local-pg(5439)·타 에이전트 pg(5440) 무접촉·무손상 실측**.

## 미완/후속
- ~~서명 이미지 실렌더(위 백엔드 요청 대기).~~ **완료**(위 "서명 이미지 실렌더" 참고).
- 시드 이중 실행으로 생긴 확인서/서류 중복은 검증용 임시 데이터(정리됨) — 소스 무관.
