# ===== 작업온 웹(Next.js) 환경변수 예시 =====
# 실제 값은 .env.local 에 복사해서 채운다 (.env*.local 은 커밋 금지).

# 백엔드 API 베이스 URL (전역 prefix /api 포함).
# 로컬 개발: 백엔드 PORT 에 맞춘다. 배포 시 실제 API 호스트로 교체.
NEXT_PUBLIC_API_URL=http://localhost:3010/api
